# Brand icons

Copied verbatim from `fe/src/lib/components/icons/` in the TextQL app so the
capability rows in this example render the same glyphs a customer sees in
Settings. These are the ones with no Material Design Icons equivalent —
everything else in `$lib/featureIcons` resolves through `~icons/mdi/*`.

Re-copy rather than edit if the app's versions change.
