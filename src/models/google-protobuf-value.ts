/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { smartUnion } from "../types/smart-union.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * `Value` represents a dynamically typed value which can be either
 *
 * @remarks
 *  null, a number, a string, a boolean, a recursive struct value, or a
 *  list of values. A producer of value is expected to set one of these
 *  variants. Absence of any variant indicates an error.
 *
 *  The JSON representation for `Value` is JSON value.
 */
export type GoogleProtobufValue = number | string | boolean | Array<any> | {
  [k: string]: any;
};

/** @internal */
export const GoogleProtobufValue$inboundSchema: z.ZodMiniType<
  GoogleProtobufValue,
  unknown
> = smartUnion([
  types.number(),
  types.string(),
  types.boolean(),
  z.array(z.any()),
  z.record(z.string(), z.any()),
]);

export function googleProtobufValueFromJSON(
  jsonString: string,
): SafeParseResult<GoogleProtobufValue, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GoogleProtobufValue$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GoogleProtobufValue' from JSON`,
  );
}
