/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { EventStream } from "../../lib/event-streams.js";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { smartUnion } from "../../types/smart-union.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import * as models from "../index.js";

export type ChatServiceStreamChatRequest = {
  chatId: string;
  body: models.TextqlRpcPublicChatChatServiceStreamChatBody;
};

/**
 * One stream envelope: `result` carries the next cell, `error` reports a failure that ended the stream.
 */
export type StreamChatEventData = {
  result?: models.TextqlRpcPublicChatCell | undefined;
  error?: models.GoogleRpcStatus | undefined;
};

/**
 * A successful response.(streaming responses)
 */
export type StreamChatEvent = {
  /**
   * One stream envelope: `result` carries the next cell, `error` reports a failure that ended the stream.
   */
  data: StreamChatEventData;
};

export type ChatServiceStreamChatResponse =
  | EventStream<StreamChatEvent>
  | models.GoogleRpcStatus;

/** @internal */
export type ChatServiceStreamChatRequest$Outbound = {
  chat_id: string;
  body: models.TextqlRpcPublicChatChatServiceStreamChatBody$Outbound;
};

/** @internal */
export const ChatServiceStreamChatRequest$outboundSchema: z.ZodMiniType<
  ChatServiceStreamChatRequest$Outbound,
  ChatServiceStreamChatRequest
> = z.pipe(
  z.object({
    chatId: z.string(),
    body: models.TextqlRpcPublicChatChatServiceStreamChatBody$outboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      chatId: "chat_id",
    });
  }),
);

export function chatServiceStreamChatRequestToJSON(
  chatServiceStreamChatRequest: ChatServiceStreamChatRequest,
): string {
  return JSON.stringify(
    ChatServiceStreamChatRequest$outboundSchema.parse(
      chatServiceStreamChatRequest,
    ),
  );
}

/** @internal */
export const StreamChatEventData$inboundSchema: z.ZodMiniType<
  StreamChatEventData,
  unknown
> = z.object({
  result: types.optional(models.TextqlRpcPublicChatCell$inboundSchema),
  error: types.optional(models.GoogleRpcStatus$inboundSchema),
});

export function streamChatEventDataFromJSON(
  jsonString: string,
): SafeParseResult<StreamChatEventData, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => StreamChatEventData$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'StreamChatEventData' from JSON`,
  );
}

/** @internal */
export const StreamChatEvent$inboundSchema: z.ZodMiniType<
  StreamChatEvent,
  unknown
> = z.object({
  data: z.pipe(
    z.pipe(
      z.unknown(),
      z.transform((v, ctx) => {
        if (typeof v !== "string") return v;
        try {
          return JSON.parse(v);
        } catch (err) {
          ctx.issues.push({
            input: v,
            code: "custom",
            message: `malformed json: ${err}`,
          });
          return z.NEVER;
        }
      }),
    ),
    z.lazy(() => StreamChatEventData$inboundSchema),
  ),
});

export function streamChatEventFromJSON(
  jsonString: string,
): SafeParseResult<StreamChatEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => StreamChatEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'StreamChatEvent' from JSON`,
  );
}

/** @internal */
export const ChatServiceStreamChatResponse$inboundSchema: z.ZodMiniType<
  ChatServiceStreamChatResponse,
  unknown
> = smartUnion([
  z.pipe(
    z.custom<ReadableStream<Uint8Array>>(x => x instanceof ReadableStream),
    z.transform(stream => {
      return new EventStream(stream, rawEvent => {
        return {
          done: false,
          value: z.lazy(() => StreamChatEvent$inboundSchema).parse(rawEvent),
        };
      });
    }),
  ),
  models.GoogleRpcStatus$inboundSchema,
]);

export function chatServiceStreamChatResponseFromJSON(
  jsonString: string,
): SafeParseResult<ChatServiceStreamChatResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ChatServiceStreamChatResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ChatServiceStreamChatResponse' from JSON`,
  );
}
