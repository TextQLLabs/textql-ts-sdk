/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  TextqlRpcParadigmParamsParadigmParams,
  TextqlRpcParadigmParamsParadigmParams$inboundSchema,
} from "./textql-rpc-paradigm-params-paradigm-params.js";

export type TextqlRpcAuthMember = {
  id?: number | undefined;
  orgId?: string | undefined;
  memberId?: string | undefined;
  role?: string | null | undefined;
  preferredFirstName?: string | null | undefined;
  preferredLastName?: string | null | undefined;
  showCode?: boolean | null | undefined;
  profileImageUrl?: string | null | undefined;
  emailAddress?: string | undefined;
  name?: string | null | undefined;
  isAdmin?: boolean | undefined;
  selectedConnectorId?: number | null | undefined;
  roles?: Array<string> | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  updatedAt?: Date | undefined;
  paradigmParams?: TextqlRpcParadigmParamsParadigmParams | undefined;
  emailVerified?: boolean | undefined;
  selectedConnectorIds?: Array<number> | undefined;
  isServiceAccount?: boolean | undefined;
  isScimManaged?: boolean | undefined;
  steering?: boolean | null | undefined;
  phoneNumber?: string | null | undefined;
  phoneVerified?: boolean | null | undefined;
  personalAgentId?: string | null | undefined;
  agentNumber?: string | null | undefined;
  /**
   * Per-member default response methodology for new chats, as a
   *
   * @remarks
   *  textql.rpc.public.chat.Methodology enum value (int to avoid an import
   *  cycle). 0 = UNKNOWN/unset -> fall back to the org default. Overrides the
   *  org default when set.
   */
  defaultMethodology?: number | null | undefined;
  /**
   * Per-member default LLM model for new chats, as a
   *
   * @remarks
   *  textql.rpc.public.chat.LlmModel enum value (int, matching the org-level
   *  default_llm_model). 0/MODEL_DEFAULT = unset -> role/org/system resolution.
   */
  defaultLlmModel?: number | null | undefined;
  /**
   * Per-member default fast-mode preference for new chats. Unset -> off. Only
   *
   * @remarks
   *  applies when the org enables fast mode and the resolved model supports it.
   */
  defaultFastMode?: boolean | null | undefined;
  /**
   * Identity (auth-provider) names, distinct from the user-set preferred names.
   */
  firstName?: string | null | undefined;
  lastName?: string | null | undefined;
  /**
   * Per-member UI theme preference: "light" | "dark" | "system".
   *
   * @remarks
   *  Unset -> the client falls back to its own default. Scoped to the
   *  membership, so a user in multiple orgs sets this per org.
   */
  themeMode?: string | null | undefined;
  /**
   * When this member creates a data app from a chat, share that builder chat
   *
   * @remarks
   *  (read-only) with everyone who can view the app. Unset -> off (opt-in).
   *  Snapshotted onto the app at creation time; later toggles only affect future apps.
   */
  shareAppBuilderChat?: boolean | null | undefined;
};

/** @internal */
export const TextqlRpcAuthMember$inboundSchema: z.ZodMiniType<
  TextqlRpcAuthMember,
  unknown
> = z.object({
  id: types.optional(types.number()),
  orgId: types.optional(types.string()),
  memberId: types.optional(types.string()),
  role: z.optional(z.nullable(types.string())),
  preferredFirstName: z.optional(z.nullable(types.string())),
  preferredLastName: z.optional(z.nullable(types.string())),
  showCode: z.optional(z.nullable(types.boolean())),
  profileImageUrl: z.optional(z.nullable(types.string())),
  emailAddress: types.optional(types.string()),
  name: z.optional(z.nullable(types.string())),
  isAdmin: types.optional(types.boolean()),
  selectedConnectorId: z.optional(z.nullable(types.number())),
  roles: types.optional(z.array(types.string())),
  createdAt: types.optional(types.date()),
  updatedAt: types.optional(types.date()),
  paradigmParams: types.optional(
    TextqlRpcParadigmParamsParadigmParams$inboundSchema,
  ),
  emailVerified: types.optional(types.boolean()),
  selectedConnectorIds: types.optional(z.array(types.number())),
  isServiceAccount: types.optional(types.boolean()),
  isScimManaged: types.optional(types.boolean()),
  steering: z.optional(z.nullable(types.boolean())),
  phoneNumber: z.optional(z.nullable(types.string())),
  phoneVerified: z.optional(z.nullable(types.boolean())),
  personalAgentId: z.optional(z.nullable(types.string())),
  agentNumber: z.optional(z.nullable(types.string())),
  defaultMethodology: z.optional(z.nullable(types.number())),
  defaultLlmModel: z.optional(z.nullable(types.number())),
  defaultFastMode: z.optional(z.nullable(types.boolean())),
  firstName: z.optional(z.nullable(types.string())),
  lastName: z.optional(z.nullable(types.string())),
  themeMode: z.optional(z.nullable(types.string())),
  shareAppBuilderChat: z.optional(z.nullable(types.boolean())),
});

export function textqlRpcAuthMemberFromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcAuthMember, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TextqlRpcAuthMember$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcAuthMember' from JSON`,
  );
}
