/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * ComputeFunction is a declared server-side function invocable from the app via the bridge.
 *
 * @remarks
 *  Exactly one of code (python, runs on the app's worker), sql (plain SQL on the app's
 *  private DB), tql (inline TQL source), or tql_path (a Context Library .tql) must be set.
 *  TQL variants are real TQL (compiled at save time) executed against a connector; sql is
 *  the app-state path (:name params bound server-side, reserved :_now / :_uuid).
 */
export type TextqlRpcPublicAppInvokeAppComputeFunctionResponse = {
  resultJson?: string | undefined;
};

/** @internal */
export const TextqlRpcPublicAppInvokeAppComputeFunctionResponse$inboundSchema:
  z.ZodMiniType<TextqlRpcPublicAppInvokeAppComputeFunctionResponse, unknown> = z
    .object({
      resultJson: types.optional(types.string()),
    });

export function textqlRpcPublicAppInvokeAppComputeFunctionResponseFromJSON(
  jsonString: string,
): SafeParseResult<
  TextqlRpcPublicAppInvokeAppComputeFunctionResponse,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicAppInvokeAppComputeFunctionResponse$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'TextqlRpcPublicAppInvokeAppComputeFunctionResponse' from JSON`,
  );
}
