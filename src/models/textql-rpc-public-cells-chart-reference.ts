/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * ChartReference points to an interactive HTML chart produced by sandbox
 *
 * @remarks
 *  code via the python cell's chart library (currently pyecharts). The chart
 *  is a self-contained HTML page (`html_url`) that loads its JS chart library
 *  from a same-origin self-hosted asset; the FE iframes this URL. A PNG
 *  rasterisation (`png_url`) is produced by the existing HTML→PNG screenshot
 *  service for the LLM's vision review, the in-chat PreviewCell thumbnail,
 *  downloads, and any non-JS surface.
 */
export type TextqlRpcPublicCellsChartReference = {
  /**
   * Internal filename (e.g. echarts_chart_<slug>.html)
   */
  name?: string | undefined;
  /**
   * Backwards-compat alias for html_url
   */
  url?: string | undefined;
  /**
   * s3 URL of the interactive HTML page
   */
  htmlUrl?: string | undefined;
  /**
   * s3 URL of the rasterised PNG screenshot
   */
  pngUrl?: string | undefined;
  /**
   * Human-readable title from the chart's option spec
   */
  title?: string | undefined;
  /**
   * s3 URL of the declarative ECharts option JSON sidecar; empty when the chart uses JsCode and is HTML-only
   */
  specUrl?: string | undefined;
};

/** @internal */
export const TextqlRpcPublicCellsChartReference$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicCellsChartReference,
  unknown
> = z.object({
  name: types.optional(types.string()),
  url: types.optional(types.string()),
  htmlUrl: types.optional(types.string()),
  pngUrl: types.optional(types.string()),
  title: types.optional(types.string()),
  specUrl: types.optional(types.string()),
});

export function textqlRpcPublicCellsChartReferenceFromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicCellsChartReference, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicCellsChartReference$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicCellsChartReference' from JSON`,
  );
}
