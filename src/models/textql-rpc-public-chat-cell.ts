/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { smartUnion } from "../types/smart-union.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  TextqlRpcPublicCellsAnswerCell,
  TextqlRpcPublicCellsAnswerCell$inboundSchema,
} from "./textql-rpc-public-cells-answer-cell.js";
import {
  TextqlRpcPublicCellsAppCell,
  TextqlRpcPublicCellsAppCell$inboundSchema,
} from "./textql-rpc-public-cells-app-cell.js";
import {
  TextqlRpcPublicCellsBashCell,
  TextqlRpcPublicCellsBashCell$inboundSchema,
} from "./textql-rpc-public-cells-bash-cell.js";
import {
  TextqlRpcPublicCellsCompactionCell,
  TextqlRpcPublicCellsCompactionCell$inboundSchema,
} from "./textql-rpc-public-cells-compaction-cell.js";
import {
  TextqlRpcPublicCellsConnectorsCell,
  TextqlRpcPublicCellsConnectorsCell$inboundSchema,
} from "./textql-rpc-public-cells-connectors-cell.js";
import {
  TextqlRpcPublicCellsContextPromptEditorCell,
  TextqlRpcPublicCellsContextPromptEditorCell$inboundSchema,
} from "./textql-rpc-public-cells-context-prompt-editor-cell.js";
import {
  TextqlRpcPublicCellsDashboardCell,
  TextqlRpcPublicCellsDashboardCell$inboundSchema,
} from "./textql-rpc-public-cells-dashboard-cell.js";
import {
  TextqlRpcPublicCellsDocumentCell,
  TextqlRpcPublicCellsDocumentCell$inboundSchema,
} from "./textql-rpc-public-cells-document-cell.js";
import {
  TextqlRpcPublicCellsEmailCell,
  TextqlRpcPublicCellsEmailCell$inboundSchema,
} from "./textql-rpc-public-cells-email-cell.js";
import {
  TextqlRpcPublicCellsFeedCommentCell,
  TextqlRpcPublicCellsFeedCommentCell$inboundSchema,
} from "./textql-rpc-public-cells-feed-comment-cell.js";
import {
  TextqlRpcPublicCellsFeedCreateCell,
  TextqlRpcPublicCellsFeedCreateCell$inboundSchema,
} from "./textql-rpc-public-cells-feed-create-cell.js";
import {
  TextqlRpcPublicCellsFeedEngageCell,
  TextqlRpcPublicCellsFeedEngageCell$inboundSchema,
} from "./textql-rpc-public-cells-feed-engage-cell.js";
import {
  TextqlRpcPublicCellsFeedExplorerCell,
  TextqlRpcPublicCellsFeedExplorerCell$inboundSchema,
} from "./textql-rpc-public-cells-feed-explorer-cell.js";
import {
  TextqlRpcPublicCellsFeedPostCell,
  TextqlRpcPublicCellsFeedPostCell$inboundSchema,
} from "./textql-rpc-public-cells-feed-post-cell.js";
import {
  TextqlRpcPublicCellsFormCell,
  TextqlRpcPublicCellsFormCell$inboundSchema,
} from "./textql-rpc-public-cells-form-cell.js";
import {
  TextqlRpcPublicCellsFormEditorCell,
  TextqlRpcPublicCellsFormEditorCell$inboundSchema,
} from "./textql-rpc-public-cells-form-editor-cell.js";
import {
  TextqlRpcPublicCellsGmailEmailContentCell,
  TextqlRpcPublicCellsGmailEmailContentCell$inboundSchema,
} from "./textql-rpc-public-cells-gmail-email-content-cell.js";
import {
  TextqlRpcPublicCellsGmailEmailSearchCell,
  TextqlRpcPublicCellsGmailEmailSearchCell$inboundSchema,
} from "./textql-rpc-public-cells-gmail-email-search-cell.js";
import {
  TextqlRpcPublicCellsGoogleCalendarSearchCell,
  TextqlRpcPublicCellsGoogleCalendarSearchCell$inboundSchema,
} from "./textql-rpc-public-cells-google-calendar-search-cell.js";
import {
  TextqlRpcPublicCellsGoogleDriveContentCell,
  TextqlRpcPublicCellsGoogleDriveContentCell$inboundSchema,
} from "./textql-rpc-public-cells-google-drive-content-cell.js";
import {
  TextqlRpcPublicCellsGoogleDriveSearchCell,
  TextqlRpcPublicCellsGoogleDriveSearchCell$inboundSchema,
} from "./textql-rpc-public-cells-google-drive-search-cell.js";
import {
  TextqlRpcPublicCellsImageCell,
  TextqlRpcPublicCellsImageCell$inboundSchema,
} from "./textql-rpc-public-cells-image-cell.js";
import {
  TextqlRpcPublicCellsJavaScriptCell,
  TextqlRpcPublicCellsJavaScriptCell$inboundSchema,
} from "./textql-rpc-public-cells-java-script-cell.js";
import {
  TextqlRpcPublicCellsLinkedinSearchCell,
  TextqlRpcPublicCellsLinkedinSearchCell$inboundSchema,
} from "./textql-rpc-public-cells-linkedin-search-cell.js";
import {
  TextqlRpcPublicCellsListAppsCell,
  TextqlRpcPublicCellsListAppsCell$inboundSchema,
} from "./textql-rpc-public-cells-list-apps-cell.js";
import {
  TextqlRpcPublicCellsListDashboardsCell,
  TextqlRpcPublicCellsListDashboardsCell$inboundSchema,
} from "./textql-rpc-public-cells-list-dashboards-cell.js";
import {
  TextqlRpcPublicCellsListUsersCell,
  TextqlRpcPublicCellsListUsersCell$inboundSchema,
} from "./textql-rpc-public-cells-list-users-cell.js";
import {
  TextqlRpcPublicCellsMarkdownCell,
  TextqlRpcPublicCellsMarkdownCell$inboundSchema,
} from "./textql-rpc-public-cells-markdown-cell.js";
import {
  TextqlRpcPublicCellsMCPToolCell,
  TextqlRpcPublicCellsMCPToolCell$inboundSchema,
} from "./textql-rpc-public-cells-mcp-tool-cell.js";
import {
  TextqlRpcPublicCellsMetricsCell,
  TextqlRpcPublicCellsMetricsCell$inboundSchema,
} from "./textql-rpc-public-cells-metrics-cell.js";
import {
  TextqlRpcPublicCellsMicrosoft365CalendarCell,
  TextqlRpcPublicCellsMicrosoft365CalendarCell$inboundSchema,
} from "./textql-rpc-public-cells-microsoft365-calendar-cell.js";
import {
  TextqlRpcPublicCellsMicrosoft365EmailContentCell,
  TextqlRpcPublicCellsMicrosoft365EmailContentCell$inboundSchema,
} from "./textql-rpc-public-cells-microsoft365-email-content-cell.js";
import {
  TextqlRpcPublicCellsMicrosoft365EmailSearchCell,
  TextqlRpcPublicCellsMicrosoft365EmailSearchCell$inboundSchema,
} from "./textql-rpc-public-cells-microsoft365-email-search-cell.js";
import {
  TextqlRpcPublicCellsOntologyEditorCell,
  TextqlRpcPublicCellsOntologyEditorCell$inboundSchema,
} from "./textql-rpc-public-cells-ontology-editor-cell.js";
import {
  TextqlRpcPublicCellsOntologyOpenObjectCell,
  TextqlRpcPublicCellsOntologyOpenObjectCell$inboundSchema,
} from "./textql-rpc-public-cells-ontology-open-object-cell.js";
import {
  TextqlRpcPublicCellsOntologyQueryCell,
  TextqlRpcPublicCellsOntologyQueryCell$inboundSchema,
} from "./textql-rpc-public-cells-ontology-query-cell.js";
import {
  TextqlRpcPublicCellsOntologySearchMetricsCell,
  TextqlRpcPublicCellsOntologySearchMetricsCell$inboundSchema,
} from "./textql-rpc-public-cells-ontology-search-metrics-cell.js";
import {
  TextqlRpcPublicCellsPatchCell,
  TextqlRpcPublicCellsPatchCell$inboundSchema,
} from "./textql-rpc-public-cells-patch-cell.js";
import {
  TextqlRpcPublicCellsPlaybookEditorCell,
  TextqlRpcPublicCellsPlaybookEditorCell$inboundSchema,
} from "./textql-rpc-public-cells-playbook-editor-cell.js";
import {
  TextqlRpcPublicCellsPowerBICell,
  TextqlRpcPublicCellsPowerBICell$inboundSchema,
} from "./textql-rpc-public-cells-power-bi-cell.js";
import {
  TextqlRpcPublicCellsPowerBIDAXCell,
  TextqlRpcPublicCellsPowerBIDAXCell$inboundSchema,
} from "./textql-rpc-public-cells-power-bidax-cell.js";
import {
  TextqlRpcPublicCellsPreviewCell,
  TextqlRpcPublicCellsPreviewCell$inboundSchema,
} from "./textql-rpc-public-cells-preview-cell.js";
import {
  TextqlRpcPublicCellsPythonCell,
  TextqlRpcPublicCellsPythonCell$inboundSchema,
} from "./textql-rpc-public-cells-python-cell.js";
import {
  TextqlRpcPublicCellsQuestionsCell,
  TextqlRpcPublicCellsQuestionsCell$inboundSchema,
} from "./textql-rpc-public-cells-questions-cell.js";
import {
  TextqlRpcPublicCellsReportCell,
  TextqlRpcPublicCellsReportCell$inboundSchema,
} from "./textql-rpc-public-cells-report-cell.js";
import {
  TextqlRpcPublicCellsReportHistoryCell,
  TextqlRpcPublicCellsReportHistoryCell$inboundSchema,
} from "./textql-rpc-public-cells-report-history-cell.js";
import {
  TextqlRpcPublicCellsSQLCell,
  TextqlRpcPublicCellsSQLCell$inboundSchema,
} from "./textql-rpc-public-cells-sql-cell.js";
import {
  TextqlRpcPublicCellsStatusCell,
  TextqlRpcPublicCellsStatusCell$inboundSchema,
} from "./textql-rpc-public-cells-status-cell.js";
import {
  TextqlRpcPublicCellsStreamlitCell,
  TextqlRpcPublicCellsStreamlitCell$inboundSchema,
} from "./textql-rpc-public-cells-streamlit-cell.js";
import {
  TextqlRpcPublicCellsSummaryCell,
  TextqlRpcPublicCellsSummaryCell$inboundSchema,
} from "./textql-rpc-public-cells-summary-cell.js";
import {
  TextqlRpcPublicCellsTableauCell,
  TextqlRpcPublicCellsTableauCell$inboundSchema,
} from "./textql-rpc-public-cells-tableau-cell.js";
import {
  TextqlRpcPublicCellsTableauSearchFieldsCell,
  TextqlRpcPublicCellsTableauSearchFieldsCell$inboundSchema,
} from "./textql-rpc-public-cells-tableau-search-fields-cell.js";
import {
  TextqlRpcPublicCellsTableauSQLCell,
  TextqlRpcPublicCellsTableauSQLCell$inboundSchema,
} from "./textql-rpc-public-cells-tableau-sql-cell.js";
import {
  TextqlRpcPublicCellsTabularFileCell,
  TextqlRpcPublicCellsTabularFileCell$inboundSchema,
} from "./textql-rpc-public-cells-tabular-file-cell.js";
import {
  TextqlRpcPublicCellsTextCell,
  TextqlRpcPublicCellsTextCell$inboundSchema,
} from "./textql-rpc-public-cells-text-cell.js";
import {
  TextqlRpcPublicCellsUseSkillCell,
  TextqlRpcPublicCellsUseSkillCell$inboundSchema,
} from "./textql-rpc-public-cells-use-skill-cell.js";
import {
  TextqlRpcPublicCellsWebSearchCell,
  TextqlRpcPublicCellsWebSearchCell$inboundSchema,
} from "./textql-rpc-public-cells-web-search-cell.js";
import {
  TextqlRpcPublicChatCellLifecycle,
  TextqlRpcPublicChatCellLifecycle$inboundSchema,
} from "./textql-rpc-public-chat-cell-lifecycle.js";

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs57 = number | string;

export type WsCell = {
  wsCell: TextqlRpcPublicCellsWebSearchCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs56 = number | string;

export type UseSkillCell = {
  /**
   * UseSkillCell is the client projection of a `use_skill` auto-invoke. It
   *
   * @remarks
   *  deliberately carries no body field: the skill's instructions are LLM-facing
   *  prompt scaffolding (see compute/pkg/chat/cells/use_skill.go), never sent to
   *  the transcript. The frontend renders provenance only ("Using skill /trigger").
   */
  useSkillCell: TextqlRpcPublicCellsUseSkillCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs55 = number | string;

export type TextCell = {
  textCell: TextqlRpcPublicCellsTextCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs54 = number | string;

export type TabularFileCell = {
  tabularFileCell: TextqlRpcPublicCellsTabularFileCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs53 = number | string;

export type TableauSqlCell = {
  tableauSqlCell: TextqlRpcPublicCellsTableauSQLCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs52 = number | string;

export type TableauSearchFieldsCell = {
  tableauSearchFieldsCell: TextqlRpcPublicCellsTableauSearchFieldsCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs51 = number | string;

export type TableauCell = {
  tableauCell: TextqlRpcPublicCellsTableauCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs50 = number | string;

export type SummaryCell = {
  summaryCell: TextqlRpcPublicCellsSummaryCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs49 = number | string;

export type StreamlitCell = {
  streamlitCell: TextqlRpcPublicCellsStreamlitCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs48 = number | string;

export type StatusCell = {
  /**
   * Deprecated: use tool_summary on Cell instead.
   */
  statusCell: TextqlRpcPublicCellsStatusCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs47 = number | string;

export type SqlCell = {
  sqlCell: TextqlRpcPublicCellsSQLCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs46 = number | string;

export type ReportHistoryCell = {
  reportHistoryCell: TextqlRpcPublicCellsReportHistoryCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs45 = number | string;

export type ReportCell = {
  reportCell: TextqlRpcPublicCellsReportCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs44 = number | string;

export type QuestionsCell = {
  /**
   * QuestionsCell is the agent's "ask the user structured questions" tool. It is a
   *
   * @remarks
   *  haltable cell: the agent pauses until the user submits or dismisses inline.
   *  On submit the answers go to the agent; on dismiss only the answered count does
   *  and the agent waits for the user's next message (the dismissal reason).
   */
  questionsCell: TextqlRpcPublicCellsQuestionsCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs43 = number | string;

export type PyCell = {
  pyCell: TextqlRpcPublicCellsPythonCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs42 = number | string;

export type PreviewCell = {
  /**
   * primary interface for ana to render sandbox assets client side
   */
  previewCell: TextqlRpcPublicCellsPreviewCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs41 = number | string;

export type PowerbiDaxCell = {
  powerbiDaxCell: TextqlRpcPublicCellsPowerBIDAXCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs40 = number | string;

export type PowerbiCell = {
  powerbiCell: TextqlRpcPublicCellsPowerBICell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs39 = number | string;

export type PlaybookEditorCell = {
  playbookEditorCell: TextqlRpcPublicCellsPlaybookEditorCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs38 = number | string;

export type PatchCell = {
  patchCell: TextqlRpcPublicCellsPatchCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs37 = number | string;

export type OntologySearchMetricsCell = {
  ontologySearchMetricsCell: TextqlRpcPublicCellsOntologySearchMetricsCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs36 = number | string;

export type OntologyQueryCell = {
  ontologyQueryCell: TextqlRpcPublicCellsOntologyQueryCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs35 = number | string;

export type OntologyOpenObjectCell = {
  ontologyOpenObjectCell: TextqlRpcPublicCellsOntologyOpenObjectCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs34 = number | string;

export type OntologyEditorCell = {
  ontologyEditorCell: TextqlRpcPublicCellsOntologyEditorCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs33 = number | string;

export type Microsoft365EmailSearchCell = {
  microsoft365EmailSearchCell: TextqlRpcPublicCellsMicrosoft365EmailSearchCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs32 = number | string;

export type Microsoft365EmailContentCell = {
  microsoft365EmailContentCell:
    TextqlRpcPublicCellsMicrosoft365EmailContentCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs31 = number | string;

export type Microsoft365CalendarCell = {
  microsoft365CalendarCell: TextqlRpcPublicCellsMicrosoft365CalendarCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs30 = number | string;

export type MetricsCell = {
  metricsCell: TextqlRpcPublicCellsMetricsCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs29 = number | string;

export type MdCell = {
  mdCell: TextqlRpcPublicCellsMarkdownCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs28 = number | string;

export type McpToolCell = {
  mcpToolCell: TextqlRpcPublicCellsMCPToolCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs27 = number | string;

export type ListUsersCell = {
  listUsersCell: TextqlRpcPublicCellsListUsersCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs26 = number | string;

export type ListDashboardsCell = {
  listDashboardsCell: TextqlRpcPublicCellsListDashboardsCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs25 = number | string;

export type ListAppsCell = {
  listAppsCell: TextqlRpcPublicCellsListAppsCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs24 = number | string;

export type LinkedinSearchCell = {
  linkedinSearchCell: TextqlRpcPublicCellsLinkedinSearchCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs23 = number | string;

export type JavascriptCell = {
  javascriptCell: TextqlRpcPublicCellsJavaScriptCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs22 = number | string;

export type ImageCell = {
  imageCell: TextqlRpcPublicCellsImageCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs21 = number | string;

export type GoogleDriveSearchCell = {
  googleDriveSearchCell: TextqlRpcPublicCellsGoogleDriveSearchCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs20 = number | string;

export type GoogleDriveContentCell = {
  googleDriveContentCell: TextqlRpcPublicCellsGoogleDriveContentCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs19 = number | string;

export type GoogleCalendarSearchCell = {
  googleCalendarSearchCell: TextqlRpcPublicCellsGoogleCalendarSearchCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs18 = number | string;

export type GmailEmailSearchCell = {
  gmailEmailSearchCell: TextqlRpcPublicCellsGmailEmailSearchCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs17 = number | string;

export type GmailEmailContentCell = {
  gmailEmailContentCell: TextqlRpcPublicCellsGmailEmailContentCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs16 = number | string;

export type FormEditorCell = {
  formEditorCell: TextqlRpcPublicCellsFormEditorCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs15 = number | string;

export type FormCell = {
  /**
   * FormCell is the v2 form editor cell. It only references a form_v5 row by id;
   *
   * @remarks
   *  the frontend loads the full form via FormService (no chat-cell scanning). The
   *  cached fields let the inline chat cell render without a round-trip.
   */
  formCell: TextqlRpcPublicCellsFormCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs14 = number | string;

export type FeedPostCell = {
  feedPostCell: TextqlRpcPublicCellsFeedPostCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs13 = number | string;

export type FeedExplorerCell = {
  feedExplorerCell: TextqlRpcPublicCellsFeedExplorerCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs12 = number | string;

export type FeedEngageCell = {
  feedEngageCell: TextqlRpcPublicCellsFeedEngageCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs11 = number | string;

export type FeedCreateCell = {
  feedCreateCell: TextqlRpcPublicCellsFeedCreateCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs10 = number | string;

export type FeedCommentCell = {
  feedCommentCell: TextqlRpcPublicCellsFeedCommentCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs9 = number | string;

export type EmailCell = {
  /**
   * EmailCell is the agent's "send an email" output. It is an executable cell:
   *
   * @remarks
   *  the LLM emits the input (to/subject/body) and the framework executes the
   *  send, mutating the result fields. The cell renders as a transcript ("Email
   *  sent to maya@acme.com at 2:14pm") with the body visible after the fact.
   */
  emailCell: TextqlRpcPublicCellsEmailCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs8 = number | string;

export type DocumentCell = {
  documentCell: TextqlRpcPublicCellsDocumentCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs7 = number | string;

export type DashboardCell = {
  dashboardCell: TextqlRpcPublicCellsDashboardCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs6 = number | string;

export type ContextPromptEditorCell = {
  contextPromptEditorCell: TextqlRpcPublicCellsContextPromptEditorCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs5 = number | string;

export type ConnectorsCell = {
  /**
   * ConnectorsCell is the agent-only "connectors" inspect tool. The frontend only
   *
   * @remarks
   *  shows that the tool ran (and a count); connector detail goes to the LLM, never
   *  to the browser, and never carries secrets.
   */
  connectorsCell: TextqlRpcPublicCellsConnectorsCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs4 = number | string;

export type CompactionCell = {
  compactionCell: TextqlRpcPublicCellsCompactionCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs3 = number | string;

export type BashCell = {
  bashCell: TextqlRpcPublicCellsBashCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs2 = number | string;

export type AppCell = {
  /**
   * AppCell records an agent action on a data app (the generative app execution primitive; apps are first-class rows in the apps table).
   */
  appCell: TextqlRpcPublicCellsAppCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

/**
 * cells_v5.duration_ms — wall-clock this cell took
 */
export type TextqlRpcPublicChatCellDurationMs1 = number | string;

export type AnsCell = {
  ansCell: TextqlRpcPublicCellsAnswerCell;
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  complete?: boolean | undefined;
  /**
   * whether it was created by a human or robot
   */
  generated?: boolean | undefined;
  lifecycle?: TextqlRpcPublicChatCellLifecycle | undefined;
  toolCallId?: string | null | undefined;
  execError?: string | null | undefined;
  senderMemberId?: string | null | undefined;
  /**
   * LLM-generated summary of what this tool call does
   */
  toolSummary?: string | null | undefined;
  /**
   * cells_v5.duration_ms — wall-clock this cell took
   */
  durationMs?: number | string | null | undefined;
};

export type TextqlRpcPublicChatCell =
  | AnsCell
  | AppCell
  | BashCell
  | CompactionCell
  | ConnectorsCell
  | ContextPromptEditorCell
  | DashboardCell
  | DocumentCell
  | EmailCell
  | FeedCommentCell
  | FeedCreateCell
  | FeedEngageCell
  | FeedExplorerCell
  | FeedPostCell
  | FormCell
  | FormEditorCell
  | GmailEmailContentCell
  | GmailEmailSearchCell
  | GoogleCalendarSearchCell
  | GoogleDriveContentCell
  | GoogleDriveSearchCell
  | ImageCell
  | JavascriptCell
  | LinkedinSearchCell
  | ListAppsCell
  | ListDashboardsCell
  | ListUsersCell
  | McpToolCell
  | MdCell
  | MetricsCell
  | Microsoft365CalendarCell
  | Microsoft365EmailContentCell
  | Microsoft365EmailSearchCell
  | OntologyEditorCell
  | OntologyOpenObjectCell
  | OntologyQueryCell
  | OntologySearchMetricsCell
  | PatchCell
  | PlaybookEditorCell
  | PowerbiCell
  | PowerbiDaxCell
  | PreviewCell
  | PyCell
  | QuestionsCell
  | ReportCell
  | ReportHistoryCell
  | SqlCell
  | StatusCell
  | StreamlitCell
  | SummaryCell
  | TableauCell
  | TableauSearchFieldsCell
  | TableauSqlCell
  | TabularFileCell
  | TextCell
  | UseSkillCell
  | WsCell;

/** @internal */
export const TextqlRpcPublicChatCellDurationMs57$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs57,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs57FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs57, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs57$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs57' from JSON`,
  );
}

/** @internal */
export const WsCell$inboundSchema: z.ZodMiniType<WsCell, unknown> = z.object({
  wsCell: TextqlRpcPublicCellsWebSearchCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function wsCellFromJSON(
  jsonString: string,
): SafeParseResult<WsCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WsCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WsCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs56$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs56,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs56FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs56, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs56$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs56' from JSON`,
  );
}

/** @internal */
export const UseSkillCell$inboundSchema: z.ZodMiniType<UseSkillCell, unknown> =
  z.object({
    useSkillCell: TextqlRpcPublicCellsUseSkillCell$inboundSchema,
    id: types.optional(types.string()),
    timestamp: types.optional(types.date()),
    complete: types.optional(types.boolean()),
    generated: types.optional(types.boolean()),
    lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
    toolCallId: z.optional(z.nullable(types.string())),
    execError: z.optional(z.nullable(types.string())),
    senderMemberId: z.optional(z.nullable(types.string())),
    toolSummary: z.optional(z.nullable(types.string())),
    durationMs: z.optional(
      z.nullable(smartUnion([types.number(), types.string()])),
    ),
  });

export function useSkillCellFromJSON(
  jsonString: string,
): SafeParseResult<UseSkillCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UseSkillCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UseSkillCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs55$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs55,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs55FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs55, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs55$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs55' from JSON`,
  );
}

/** @internal */
export const TextCell$inboundSchema: z.ZodMiniType<TextCell, unknown> = z
  .object({
    textCell: TextqlRpcPublicCellsTextCell$inboundSchema,
    id: types.optional(types.string()),
    timestamp: types.optional(types.date()),
    complete: types.optional(types.boolean()),
    generated: types.optional(types.boolean()),
    lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
    toolCallId: z.optional(z.nullable(types.string())),
    execError: z.optional(z.nullable(types.string())),
    senderMemberId: z.optional(z.nullable(types.string())),
    toolSummary: z.optional(z.nullable(types.string())),
    durationMs: z.optional(
      z.nullable(smartUnion([types.number(), types.string()])),
    ),
  });

export function textCellFromJSON(
  jsonString: string,
): SafeParseResult<TextCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TextCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs54$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs54,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs54FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs54, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs54$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs54' from JSON`,
  );
}

/** @internal */
export const TabularFileCell$inboundSchema: z.ZodMiniType<
  TabularFileCell,
  unknown
> = z.object({
  tabularFileCell: TextqlRpcPublicCellsTabularFileCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function tabularFileCellFromJSON(
  jsonString: string,
): SafeParseResult<TabularFileCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TabularFileCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TabularFileCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs53$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs53,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs53FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs53, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs53$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs53' from JSON`,
  );
}

/** @internal */
export const TableauSqlCell$inboundSchema: z.ZodMiniType<
  TableauSqlCell,
  unknown
> = z.object({
  tableauSqlCell: TextqlRpcPublicCellsTableauSQLCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function tableauSqlCellFromJSON(
  jsonString: string,
): SafeParseResult<TableauSqlCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TableauSqlCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TableauSqlCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs52$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs52,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs52FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs52, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs52$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs52' from JSON`,
  );
}

/** @internal */
export const TableauSearchFieldsCell$inboundSchema: z.ZodMiniType<
  TableauSearchFieldsCell,
  unknown
> = z.object({
  tableauSearchFieldsCell:
    TextqlRpcPublicCellsTableauSearchFieldsCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function tableauSearchFieldsCellFromJSON(
  jsonString: string,
): SafeParseResult<TableauSearchFieldsCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TableauSearchFieldsCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TableauSearchFieldsCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs51$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs51,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs51FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs51, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs51$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs51' from JSON`,
  );
}

/** @internal */
export const TableauCell$inboundSchema: z.ZodMiniType<TableauCell, unknown> = z
  .object({
    tableauCell: TextqlRpcPublicCellsTableauCell$inboundSchema,
    id: types.optional(types.string()),
    timestamp: types.optional(types.date()),
    complete: types.optional(types.boolean()),
    generated: types.optional(types.boolean()),
    lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
    toolCallId: z.optional(z.nullable(types.string())),
    execError: z.optional(z.nullable(types.string())),
    senderMemberId: z.optional(z.nullable(types.string())),
    toolSummary: z.optional(z.nullable(types.string())),
    durationMs: z.optional(
      z.nullable(smartUnion([types.number(), types.string()])),
    ),
  });

export function tableauCellFromJSON(
  jsonString: string,
): SafeParseResult<TableauCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TableauCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TableauCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs50$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs50,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs50FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs50, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs50$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs50' from JSON`,
  );
}

/** @internal */
export const SummaryCell$inboundSchema: z.ZodMiniType<SummaryCell, unknown> = z
  .object({
    summaryCell: TextqlRpcPublicCellsSummaryCell$inboundSchema,
    id: types.optional(types.string()),
    timestamp: types.optional(types.date()),
    complete: types.optional(types.boolean()),
    generated: types.optional(types.boolean()),
    lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
    toolCallId: z.optional(z.nullable(types.string())),
    execError: z.optional(z.nullable(types.string())),
    senderMemberId: z.optional(z.nullable(types.string())),
    toolSummary: z.optional(z.nullable(types.string())),
    durationMs: z.optional(
      z.nullable(smartUnion([types.number(), types.string()])),
    ),
  });

export function summaryCellFromJSON(
  jsonString: string,
): SafeParseResult<SummaryCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SummaryCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SummaryCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs49$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs49,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs49FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs49, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs49$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs49' from JSON`,
  );
}

/** @internal */
export const StreamlitCell$inboundSchema: z.ZodMiniType<
  StreamlitCell,
  unknown
> = z.object({
  streamlitCell: TextqlRpcPublicCellsStreamlitCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function streamlitCellFromJSON(
  jsonString: string,
): SafeParseResult<StreamlitCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => StreamlitCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'StreamlitCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs48$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs48,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs48FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs48, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs48$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs48' from JSON`,
  );
}

/** @internal */
export const StatusCell$inboundSchema: z.ZodMiniType<StatusCell, unknown> = z
  .object({
    statusCell: TextqlRpcPublicCellsStatusCell$inboundSchema,
    id: types.optional(types.string()),
    timestamp: types.optional(types.date()),
    complete: types.optional(types.boolean()),
    generated: types.optional(types.boolean()),
    lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
    toolCallId: z.optional(z.nullable(types.string())),
    execError: z.optional(z.nullable(types.string())),
    senderMemberId: z.optional(z.nullable(types.string())),
    toolSummary: z.optional(z.nullable(types.string())),
    durationMs: z.optional(
      z.nullable(smartUnion([types.number(), types.string()])),
    ),
  });

export function statusCellFromJSON(
  jsonString: string,
): SafeParseResult<StatusCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => StatusCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'StatusCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs47$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs47,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs47FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs47, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs47$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs47' from JSON`,
  );
}

/** @internal */
export const SqlCell$inboundSchema: z.ZodMiniType<SqlCell, unknown> = z.object({
  sqlCell: TextqlRpcPublicCellsSQLCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function sqlCellFromJSON(
  jsonString: string,
): SafeParseResult<SqlCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SqlCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SqlCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs46$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs46,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs46FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs46, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs46$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs46' from JSON`,
  );
}

/** @internal */
export const ReportHistoryCell$inboundSchema: z.ZodMiniType<
  ReportHistoryCell,
  unknown
> = z.object({
  reportHistoryCell: TextqlRpcPublicCellsReportHistoryCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function reportHistoryCellFromJSON(
  jsonString: string,
): SafeParseResult<ReportHistoryCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReportHistoryCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReportHistoryCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs45$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs45,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs45FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs45, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs45$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs45' from JSON`,
  );
}

/** @internal */
export const ReportCell$inboundSchema: z.ZodMiniType<ReportCell, unknown> = z
  .object({
    reportCell: TextqlRpcPublicCellsReportCell$inboundSchema,
    id: types.optional(types.string()),
    timestamp: types.optional(types.date()),
    complete: types.optional(types.boolean()),
    generated: types.optional(types.boolean()),
    lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
    toolCallId: z.optional(z.nullable(types.string())),
    execError: z.optional(z.nullable(types.string())),
    senderMemberId: z.optional(z.nullable(types.string())),
    toolSummary: z.optional(z.nullable(types.string())),
    durationMs: z.optional(
      z.nullable(smartUnion([types.number(), types.string()])),
    ),
  });

export function reportCellFromJSON(
  jsonString: string,
): SafeParseResult<ReportCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReportCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReportCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs44$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs44,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs44FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs44, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs44$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs44' from JSON`,
  );
}

/** @internal */
export const QuestionsCell$inboundSchema: z.ZodMiniType<
  QuestionsCell,
  unknown
> = z.object({
  questionsCell: TextqlRpcPublicCellsQuestionsCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function questionsCellFromJSON(
  jsonString: string,
): SafeParseResult<QuestionsCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => QuestionsCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'QuestionsCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs43$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs43,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs43FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs43, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs43$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs43' from JSON`,
  );
}

/** @internal */
export const PyCell$inboundSchema: z.ZodMiniType<PyCell, unknown> = z.object({
  pyCell: TextqlRpcPublicCellsPythonCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function pyCellFromJSON(
  jsonString: string,
): SafeParseResult<PyCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PyCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PyCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs42$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs42,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs42FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs42, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs42$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs42' from JSON`,
  );
}

/** @internal */
export const PreviewCell$inboundSchema: z.ZodMiniType<PreviewCell, unknown> = z
  .object({
    previewCell: TextqlRpcPublicCellsPreviewCell$inboundSchema,
    id: types.optional(types.string()),
    timestamp: types.optional(types.date()),
    complete: types.optional(types.boolean()),
    generated: types.optional(types.boolean()),
    lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
    toolCallId: z.optional(z.nullable(types.string())),
    execError: z.optional(z.nullable(types.string())),
    senderMemberId: z.optional(z.nullable(types.string())),
    toolSummary: z.optional(z.nullable(types.string())),
    durationMs: z.optional(
      z.nullable(smartUnion([types.number(), types.string()])),
    ),
  });

export function previewCellFromJSON(
  jsonString: string,
): SafeParseResult<PreviewCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PreviewCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PreviewCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs41$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs41,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs41FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs41, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs41$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs41' from JSON`,
  );
}

/** @internal */
export const PowerbiDaxCell$inboundSchema: z.ZodMiniType<
  PowerbiDaxCell,
  unknown
> = z.object({
  powerbiDaxCell: TextqlRpcPublicCellsPowerBIDAXCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function powerbiDaxCellFromJSON(
  jsonString: string,
): SafeParseResult<PowerbiDaxCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PowerbiDaxCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PowerbiDaxCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs40$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs40,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs40FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs40, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs40$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs40' from JSON`,
  );
}

/** @internal */
export const PowerbiCell$inboundSchema: z.ZodMiniType<PowerbiCell, unknown> = z
  .object({
    powerbiCell: TextqlRpcPublicCellsPowerBICell$inboundSchema,
    id: types.optional(types.string()),
    timestamp: types.optional(types.date()),
    complete: types.optional(types.boolean()),
    generated: types.optional(types.boolean()),
    lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
    toolCallId: z.optional(z.nullable(types.string())),
    execError: z.optional(z.nullable(types.string())),
    senderMemberId: z.optional(z.nullable(types.string())),
    toolSummary: z.optional(z.nullable(types.string())),
    durationMs: z.optional(
      z.nullable(smartUnion([types.number(), types.string()])),
    ),
  });

export function powerbiCellFromJSON(
  jsonString: string,
): SafeParseResult<PowerbiCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PowerbiCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PowerbiCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs39$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs39,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs39FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs39, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs39$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs39' from JSON`,
  );
}

/** @internal */
export const PlaybookEditorCell$inboundSchema: z.ZodMiniType<
  PlaybookEditorCell,
  unknown
> = z.object({
  playbookEditorCell: TextqlRpcPublicCellsPlaybookEditorCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function playbookEditorCellFromJSON(
  jsonString: string,
): SafeParseResult<PlaybookEditorCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PlaybookEditorCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PlaybookEditorCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs38$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs38,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs38FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs38, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs38$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs38' from JSON`,
  );
}

/** @internal */
export const PatchCell$inboundSchema: z.ZodMiniType<PatchCell, unknown> = z
  .object({
    patchCell: TextqlRpcPublicCellsPatchCell$inboundSchema,
    id: types.optional(types.string()),
    timestamp: types.optional(types.date()),
    complete: types.optional(types.boolean()),
    generated: types.optional(types.boolean()),
    lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
    toolCallId: z.optional(z.nullable(types.string())),
    execError: z.optional(z.nullable(types.string())),
    senderMemberId: z.optional(z.nullable(types.string())),
    toolSummary: z.optional(z.nullable(types.string())),
    durationMs: z.optional(
      z.nullable(smartUnion([types.number(), types.string()])),
    ),
  });

export function patchCellFromJSON(
  jsonString: string,
): SafeParseResult<PatchCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PatchCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PatchCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs37$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs37,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs37FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs37, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs37$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs37' from JSON`,
  );
}

/** @internal */
export const OntologySearchMetricsCell$inboundSchema: z.ZodMiniType<
  OntologySearchMetricsCell,
  unknown
> = z.object({
  ontologySearchMetricsCell:
    TextqlRpcPublicCellsOntologySearchMetricsCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function ontologySearchMetricsCellFromJSON(
  jsonString: string,
): SafeParseResult<OntologySearchMetricsCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OntologySearchMetricsCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OntologySearchMetricsCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs36$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs36,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs36FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs36, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs36$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs36' from JSON`,
  );
}

/** @internal */
export const OntologyQueryCell$inboundSchema: z.ZodMiniType<
  OntologyQueryCell,
  unknown
> = z.object({
  ontologyQueryCell: TextqlRpcPublicCellsOntologyQueryCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function ontologyQueryCellFromJSON(
  jsonString: string,
): SafeParseResult<OntologyQueryCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OntologyQueryCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OntologyQueryCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs35$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs35,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs35FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs35, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs35$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs35' from JSON`,
  );
}

/** @internal */
export const OntologyOpenObjectCell$inboundSchema: z.ZodMiniType<
  OntologyOpenObjectCell,
  unknown
> = z.object({
  ontologyOpenObjectCell:
    TextqlRpcPublicCellsOntologyOpenObjectCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function ontologyOpenObjectCellFromJSON(
  jsonString: string,
): SafeParseResult<OntologyOpenObjectCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OntologyOpenObjectCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OntologyOpenObjectCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs34$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs34,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs34FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs34, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs34$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs34' from JSON`,
  );
}

/** @internal */
export const OntologyEditorCell$inboundSchema: z.ZodMiniType<
  OntologyEditorCell,
  unknown
> = z.object({
  ontologyEditorCell: TextqlRpcPublicCellsOntologyEditorCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function ontologyEditorCellFromJSON(
  jsonString: string,
): SafeParseResult<OntologyEditorCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OntologyEditorCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OntologyEditorCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs33$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs33,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs33FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs33, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs33$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs33' from JSON`,
  );
}

/** @internal */
export const Microsoft365EmailSearchCell$inboundSchema: z.ZodMiniType<
  Microsoft365EmailSearchCell,
  unknown
> = z.object({
  microsoft365EmailSearchCell:
    TextqlRpcPublicCellsMicrosoft365EmailSearchCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function microsoft365EmailSearchCellFromJSON(
  jsonString: string,
): SafeParseResult<Microsoft365EmailSearchCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Microsoft365EmailSearchCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Microsoft365EmailSearchCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs32$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs32,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs32FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs32, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs32$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs32' from JSON`,
  );
}

/** @internal */
export const Microsoft365EmailContentCell$inboundSchema: z.ZodMiniType<
  Microsoft365EmailContentCell,
  unknown
> = z.object({
  microsoft365EmailContentCell:
    TextqlRpcPublicCellsMicrosoft365EmailContentCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function microsoft365EmailContentCellFromJSON(
  jsonString: string,
): SafeParseResult<Microsoft365EmailContentCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Microsoft365EmailContentCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Microsoft365EmailContentCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs31$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs31,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs31FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs31, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs31$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs31' from JSON`,
  );
}

/** @internal */
export const Microsoft365CalendarCell$inboundSchema: z.ZodMiniType<
  Microsoft365CalendarCell,
  unknown
> = z.object({
  microsoft365CalendarCell:
    TextqlRpcPublicCellsMicrosoft365CalendarCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function microsoft365CalendarCellFromJSON(
  jsonString: string,
): SafeParseResult<Microsoft365CalendarCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Microsoft365CalendarCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Microsoft365CalendarCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs30$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs30,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs30FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs30, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs30$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs30' from JSON`,
  );
}

/** @internal */
export const MetricsCell$inboundSchema: z.ZodMiniType<MetricsCell, unknown> = z
  .object({
    metricsCell: TextqlRpcPublicCellsMetricsCell$inboundSchema,
    id: types.optional(types.string()),
    timestamp: types.optional(types.date()),
    complete: types.optional(types.boolean()),
    generated: types.optional(types.boolean()),
    lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
    toolCallId: z.optional(z.nullable(types.string())),
    execError: z.optional(z.nullable(types.string())),
    senderMemberId: z.optional(z.nullable(types.string())),
    toolSummary: z.optional(z.nullable(types.string())),
    durationMs: z.optional(
      z.nullable(smartUnion([types.number(), types.string()])),
    ),
  });

export function metricsCellFromJSON(
  jsonString: string,
): SafeParseResult<MetricsCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MetricsCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MetricsCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs29$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs29,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs29FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs29, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs29$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs29' from JSON`,
  );
}

/** @internal */
export const MdCell$inboundSchema: z.ZodMiniType<MdCell, unknown> = z.object({
  mdCell: TextqlRpcPublicCellsMarkdownCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function mdCellFromJSON(
  jsonString: string,
): SafeParseResult<MdCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MdCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MdCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs28$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs28,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs28FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs28, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs28$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs28' from JSON`,
  );
}

/** @internal */
export const McpToolCell$inboundSchema: z.ZodMiniType<McpToolCell, unknown> = z
  .object({
    mcpToolCell: TextqlRpcPublicCellsMCPToolCell$inboundSchema,
    id: types.optional(types.string()),
    timestamp: types.optional(types.date()),
    complete: types.optional(types.boolean()),
    generated: types.optional(types.boolean()),
    lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
    toolCallId: z.optional(z.nullable(types.string())),
    execError: z.optional(z.nullable(types.string())),
    senderMemberId: z.optional(z.nullable(types.string())),
    toolSummary: z.optional(z.nullable(types.string())),
    durationMs: z.optional(
      z.nullable(smartUnion([types.number(), types.string()])),
    ),
  });

export function mcpToolCellFromJSON(
  jsonString: string,
): SafeParseResult<McpToolCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => McpToolCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'McpToolCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs27$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs27,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs27FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs27, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs27$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs27' from JSON`,
  );
}

/** @internal */
export const ListUsersCell$inboundSchema: z.ZodMiniType<
  ListUsersCell,
  unknown
> = z.object({
  listUsersCell: TextqlRpcPublicCellsListUsersCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function listUsersCellFromJSON(
  jsonString: string,
): SafeParseResult<ListUsersCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListUsersCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListUsersCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs26$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs26,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs26FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs26, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs26$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs26' from JSON`,
  );
}

/** @internal */
export const ListDashboardsCell$inboundSchema: z.ZodMiniType<
  ListDashboardsCell,
  unknown
> = z.object({
  listDashboardsCell: TextqlRpcPublicCellsListDashboardsCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function listDashboardsCellFromJSON(
  jsonString: string,
): SafeParseResult<ListDashboardsCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListDashboardsCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListDashboardsCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs25$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs25,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs25FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs25, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs25$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs25' from JSON`,
  );
}

/** @internal */
export const ListAppsCell$inboundSchema: z.ZodMiniType<ListAppsCell, unknown> =
  z.object({
    listAppsCell: TextqlRpcPublicCellsListAppsCell$inboundSchema,
    id: types.optional(types.string()),
    timestamp: types.optional(types.date()),
    complete: types.optional(types.boolean()),
    generated: types.optional(types.boolean()),
    lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
    toolCallId: z.optional(z.nullable(types.string())),
    execError: z.optional(z.nullable(types.string())),
    senderMemberId: z.optional(z.nullable(types.string())),
    toolSummary: z.optional(z.nullable(types.string())),
    durationMs: z.optional(
      z.nullable(smartUnion([types.number(), types.string()])),
    ),
  });

export function listAppsCellFromJSON(
  jsonString: string,
): SafeParseResult<ListAppsCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListAppsCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListAppsCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs24$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs24,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs24FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs24, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs24$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs24' from JSON`,
  );
}

/** @internal */
export const LinkedinSearchCell$inboundSchema: z.ZodMiniType<
  LinkedinSearchCell,
  unknown
> = z.object({
  linkedinSearchCell: TextqlRpcPublicCellsLinkedinSearchCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function linkedinSearchCellFromJSON(
  jsonString: string,
): SafeParseResult<LinkedinSearchCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LinkedinSearchCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LinkedinSearchCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs23$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs23,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs23FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs23, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs23$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs23' from JSON`,
  );
}

/** @internal */
export const JavascriptCell$inboundSchema: z.ZodMiniType<
  JavascriptCell,
  unknown
> = z.object({
  javascriptCell: TextqlRpcPublicCellsJavaScriptCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function javascriptCellFromJSON(
  jsonString: string,
): SafeParseResult<JavascriptCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => JavascriptCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'JavascriptCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs22$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs22,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs22FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs22, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs22$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs22' from JSON`,
  );
}

/** @internal */
export const ImageCell$inboundSchema: z.ZodMiniType<ImageCell, unknown> = z
  .object({
    imageCell: TextqlRpcPublicCellsImageCell$inboundSchema,
    id: types.optional(types.string()),
    timestamp: types.optional(types.date()),
    complete: types.optional(types.boolean()),
    generated: types.optional(types.boolean()),
    lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
    toolCallId: z.optional(z.nullable(types.string())),
    execError: z.optional(z.nullable(types.string())),
    senderMemberId: z.optional(z.nullable(types.string())),
    toolSummary: z.optional(z.nullable(types.string())),
    durationMs: z.optional(
      z.nullable(smartUnion([types.number(), types.string()])),
    ),
  });

export function imageCellFromJSON(
  jsonString: string,
): SafeParseResult<ImageCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ImageCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ImageCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs21$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs21,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs21FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs21, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs21$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs21' from JSON`,
  );
}

/** @internal */
export const GoogleDriveSearchCell$inboundSchema: z.ZodMiniType<
  GoogleDriveSearchCell,
  unknown
> = z.object({
  googleDriveSearchCell:
    TextqlRpcPublicCellsGoogleDriveSearchCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function googleDriveSearchCellFromJSON(
  jsonString: string,
): SafeParseResult<GoogleDriveSearchCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GoogleDriveSearchCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GoogleDriveSearchCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs20$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs20,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs20FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs20, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs20$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs20' from JSON`,
  );
}

/** @internal */
export const GoogleDriveContentCell$inboundSchema: z.ZodMiniType<
  GoogleDriveContentCell,
  unknown
> = z.object({
  googleDriveContentCell:
    TextqlRpcPublicCellsGoogleDriveContentCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function googleDriveContentCellFromJSON(
  jsonString: string,
): SafeParseResult<GoogleDriveContentCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GoogleDriveContentCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GoogleDriveContentCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs19$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs19,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs19FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs19, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs19$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs19' from JSON`,
  );
}

/** @internal */
export const GoogleCalendarSearchCell$inboundSchema: z.ZodMiniType<
  GoogleCalendarSearchCell,
  unknown
> = z.object({
  googleCalendarSearchCell:
    TextqlRpcPublicCellsGoogleCalendarSearchCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function googleCalendarSearchCellFromJSON(
  jsonString: string,
): SafeParseResult<GoogleCalendarSearchCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GoogleCalendarSearchCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GoogleCalendarSearchCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs18$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs18,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs18FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs18, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs18$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs18' from JSON`,
  );
}

/** @internal */
export const GmailEmailSearchCell$inboundSchema: z.ZodMiniType<
  GmailEmailSearchCell,
  unknown
> = z.object({
  gmailEmailSearchCell: TextqlRpcPublicCellsGmailEmailSearchCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function gmailEmailSearchCellFromJSON(
  jsonString: string,
): SafeParseResult<GmailEmailSearchCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GmailEmailSearchCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GmailEmailSearchCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs17$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs17,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs17FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs17, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs17$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs17' from JSON`,
  );
}

/** @internal */
export const GmailEmailContentCell$inboundSchema: z.ZodMiniType<
  GmailEmailContentCell,
  unknown
> = z.object({
  gmailEmailContentCell:
    TextqlRpcPublicCellsGmailEmailContentCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function gmailEmailContentCellFromJSON(
  jsonString: string,
): SafeParseResult<GmailEmailContentCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GmailEmailContentCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GmailEmailContentCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs16$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs16,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs16FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs16, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs16$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs16' from JSON`,
  );
}

/** @internal */
export const FormEditorCell$inboundSchema: z.ZodMiniType<
  FormEditorCell,
  unknown
> = z.object({
  formEditorCell: TextqlRpcPublicCellsFormEditorCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function formEditorCellFromJSON(
  jsonString: string,
): SafeParseResult<FormEditorCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FormEditorCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FormEditorCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs15$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs15,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs15FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs15, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs15$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs15' from JSON`,
  );
}

/** @internal */
export const FormCell$inboundSchema: z.ZodMiniType<FormCell, unknown> = z
  .object({
    formCell: TextqlRpcPublicCellsFormCell$inboundSchema,
    id: types.optional(types.string()),
    timestamp: types.optional(types.date()),
    complete: types.optional(types.boolean()),
    generated: types.optional(types.boolean()),
    lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
    toolCallId: z.optional(z.nullable(types.string())),
    execError: z.optional(z.nullable(types.string())),
    senderMemberId: z.optional(z.nullable(types.string())),
    toolSummary: z.optional(z.nullable(types.string())),
    durationMs: z.optional(
      z.nullable(smartUnion([types.number(), types.string()])),
    ),
  });

export function formCellFromJSON(
  jsonString: string,
): SafeParseResult<FormCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FormCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FormCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs14$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs14,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs14FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs14, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs14$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs14' from JSON`,
  );
}

/** @internal */
export const FeedPostCell$inboundSchema: z.ZodMiniType<FeedPostCell, unknown> =
  z.object({
    feedPostCell: TextqlRpcPublicCellsFeedPostCell$inboundSchema,
    id: types.optional(types.string()),
    timestamp: types.optional(types.date()),
    complete: types.optional(types.boolean()),
    generated: types.optional(types.boolean()),
    lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
    toolCallId: z.optional(z.nullable(types.string())),
    execError: z.optional(z.nullable(types.string())),
    senderMemberId: z.optional(z.nullable(types.string())),
    toolSummary: z.optional(z.nullable(types.string())),
    durationMs: z.optional(
      z.nullable(smartUnion([types.number(), types.string()])),
    ),
  });

export function feedPostCellFromJSON(
  jsonString: string,
): SafeParseResult<FeedPostCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FeedPostCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FeedPostCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs13$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs13,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs13FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs13, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs13$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs13' from JSON`,
  );
}

/** @internal */
export const FeedExplorerCell$inboundSchema: z.ZodMiniType<
  FeedExplorerCell,
  unknown
> = z.object({
  feedExplorerCell: TextqlRpcPublicCellsFeedExplorerCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function feedExplorerCellFromJSON(
  jsonString: string,
): SafeParseResult<FeedExplorerCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FeedExplorerCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FeedExplorerCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs12$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs12,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs12FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs12, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs12$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs12' from JSON`,
  );
}

/** @internal */
export const FeedEngageCell$inboundSchema: z.ZodMiniType<
  FeedEngageCell,
  unknown
> = z.object({
  feedEngageCell: TextqlRpcPublicCellsFeedEngageCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function feedEngageCellFromJSON(
  jsonString: string,
): SafeParseResult<FeedEngageCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FeedEngageCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FeedEngageCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs11$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs11,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs11FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs11, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs11$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs11' from JSON`,
  );
}

/** @internal */
export const FeedCreateCell$inboundSchema: z.ZodMiniType<
  FeedCreateCell,
  unknown
> = z.object({
  feedCreateCell: TextqlRpcPublicCellsFeedCreateCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function feedCreateCellFromJSON(
  jsonString: string,
): SafeParseResult<FeedCreateCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FeedCreateCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FeedCreateCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs10$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs10,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs10FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs10, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs10$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs10' from JSON`,
  );
}

/** @internal */
export const FeedCommentCell$inboundSchema: z.ZodMiniType<
  FeedCommentCell,
  unknown
> = z.object({
  feedCommentCell: TextqlRpcPublicCellsFeedCommentCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function feedCommentCellFromJSON(
  jsonString: string,
): SafeParseResult<FeedCommentCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FeedCommentCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FeedCommentCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs9$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs9,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs9FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs9, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs9$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs9' from JSON`,
  );
}

/** @internal */
export const EmailCell$inboundSchema: z.ZodMiniType<EmailCell, unknown> = z
  .object({
    emailCell: TextqlRpcPublicCellsEmailCell$inboundSchema,
    id: types.optional(types.string()),
    timestamp: types.optional(types.date()),
    complete: types.optional(types.boolean()),
    generated: types.optional(types.boolean()),
    lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
    toolCallId: z.optional(z.nullable(types.string())),
    execError: z.optional(z.nullable(types.string())),
    senderMemberId: z.optional(z.nullable(types.string())),
    toolSummary: z.optional(z.nullable(types.string())),
    durationMs: z.optional(
      z.nullable(smartUnion([types.number(), types.string()])),
    ),
  });

export function emailCellFromJSON(
  jsonString: string,
): SafeParseResult<EmailCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EmailCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EmailCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs8$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs8,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs8FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs8, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs8$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs8' from JSON`,
  );
}

/** @internal */
export const DocumentCell$inboundSchema: z.ZodMiniType<DocumentCell, unknown> =
  z.object({
    documentCell: TextqlRpcPublicCellsDocumentCell$inboundSchema,
    id: types.optional(types.string()),
    timestamp: types.optional(types.date()),
    complete: types.optional(types.boolean()),
    generated: types.optional(types.boolean()),
    lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
    toolCallId: z.optional(z.nullable(types.string())),
    execError: z.optional(z.nullable(types.string())),
    senderMemberId: z.optional(z.nullable(types.string())),
    toolSummary: z.optional(z.nullable(types.string())),
    durationMs: z.optional(
      z.nullable(smartUnion([types.number(), types.string()])),
    ),
  });

export function documentCellFromJSON(
  jsonString: string,
): SafeParseResult<DocumentCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DocumentCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DocumentCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs7$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs7,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs7FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs7, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs7$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs7' from JSON`,
  );
}

/** @internal */
export const DashboardCell$inboundSchema: z.ZodMiniType<
  DashboardCell,
  unknown
> = z.object({
  dashboardCell: TextqlRpcPublicCellsDashboardCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function dashboardCellFromJSON(
  jsonString: string,
): SafeParseResult<DashboardCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DashboardCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DashboardCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs6$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs6,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs6FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs6, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs6$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs6' from JSON`,
  );
}

/** @internal */
export const ContextPromptEditorCell$inboundSchema: z.ZodMiniType<
  ContextPromptEditorCell,
  unknown
> = z.object({
  contextPromptEditorCell:
    TextqlRpcPublicCellsContextPromptEditorCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function contextPromptEditorCellFromJSON(
  jsonString: string,
): SafeParseResult<ContextPromptEditorCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ContextPromptEditorCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ContextPromptEditorCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs5$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs5,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs5FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs5, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs5$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs5' from JSON`,
  );
}

/** @internal */
export const ConnectorsCell$inboundSchema: z.ZodMiniType<
  ConnectorsCell,
  unknown
> = z.object({
  connectorsCell: TextqlRpcPublicCellsConnectorsCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function connectorsCellFromJSON(
  jsonString: string,
): SafeParseResult<ConnectorsCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ConnectorsCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ConnectorsCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs4$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs4,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs4FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs4, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs4$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs4' from JSON`,
  );
}

/** @internal */
export const CompactionCell$inboundSchema: z.ZodMiniType<
  CompactionCell,
  unknown
> = z.object({
  compactionCell: TextqlRpcPublicCellsCompactionCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function compactionCellFromJSON(
  jsonString: string,
): SafeParseResult<CompactionCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CompactionCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CompactionCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs3$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs3,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs3FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs3, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs3$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs3' from JSON`,
  );
}

/** @internal */
export const BashCell$inboundSchema: z.ZodMiniType<BashCell, unknown> = z
  .object({
    bashCell: TextqlRpcPublicCellsBashCell$inboundSchema,
    id: types.optional(types.string()),
    timestamp: types.optional(types.date()),
    complete: types.optional(types.boolean()),
    generated: types.optional(types.boolean()),
    lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
    toolCallId: z.optional(z.nullable(types.string())),
    execError: z.optional(z.nullable(types.string())),
    senderMemberId: z.optional(z.nullable(types.string())),
    toolSummary: z.optional(z.nullable(types.string())),
    durationMs: z.optional(
      z.nullable(smartUnion([types.number(), types.string()])),
    ),
  });

export function bashCellFromJSON(
  jsonString: string,
): SafeParseResult<BashCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BashCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BashCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs2$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs2,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs2FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs2, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs2$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs2' from JSON`,
  );
}

/** @internal */
export const AppCell$inboundSchema: z.ZodMiniType<AppCell, unknown> = z.object({
  appCell: TextqlRpcPublicCellsAppCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function appCellFromJSON(
  jsonString: string,
): SafeParseResult<AppCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AppCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AppCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCellDurationMs1$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCellDurationMs1,
  unknown
> = smartUnion([types.number(), types.string()]);

export function textqlRpcPublicChatCellDurationMs1FromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCellDurationMs1, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicChatCellDurationMs1$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCellDurationMs1' from JSON`,
  );
}

/** @internal */
export const AnsCell$inboundSchema: z.ZodMiniType<AnsCell, unknown> = z.object({
  ansCell: TextqlRpcPublicCellsAnswerCell$inboundSchema,
  id: types.optional(types.string()),
  timestamp: types.optional(types.date()),
  complete: types.optional(types.boolean()),
  generated: types.optional(types.boolean()),
  lifecycle: types.optional(TextqlRpcPublicChatCellLifecycle$inboundSchema),
  toolCallId: z.optional(z.nullable(types.string())),
  execError: z.optional(z.nullable(types.string())),
  senderMemberId: z.optional(z.nullable(types.string())),
  toolSummary: z.optional(z.nullable(types.string())),
  durationMs: z.optional(
    z.nullable(smartUnion([types.number(), types.string()])),
  ),
});

export function ansCellFromJSON(
  jsonString: string,
): SafeParseResult<AnsCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AnsCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AnsCell' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicChatCell$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatCell,
  unknown
> = smartUnion([
  z.lazy(() => AnsCell$inboundSchema),
  z.lazy(() => AppCell$inboundSchema),
  z.lazy(() => BashCell$inboundSchema),
  z.lazy(() => CompactionCell$inboundSchema),
  z.lazy(() => ConnectorsCell$inboundSchema),
  z.lazy(() => ContextPromptEditorCell$inboundSchema),
  z.lazy(() => DashboardCell$inboundSchema),
  z.lazy(() => DocumentCell$inboundSchema),
  z.lazy(() => EmailCell$inboundSchema),
  z.lazy(() => FeedCommentCell$inboundSchema),
  z.lazy(() => FeedCreateCell$inboundSchema),
  z.lazy(() => FeedEngageCell$inboundSchema),
  z.lazy(() => FeedExplorerCell$inboundSchema),
  z.lazy(() => FeedPostCell$inboundSchema),
  z.lazy(() => FormCell$inboundSchema),
  z.lazy(() => FormEditorCell$inboundSchema),
  z.lazy(() => GmailEmailContentCell$inboundSchema),
  z.lazy(() => GmailEmailSearchCell$inboundSchema),
  z.lazy(() => GoogleCalendarSearchCell$inboundSchema),
  z.lazy(() => GoogleDriveContentCell$inboundSchema),
  z.lazy(() => GoogleDriveSearchCell$inboundSchema),
  z.lazy(() => ImageCell$inboundSchema),
  z.lazy(() => JavascriptCell$inboundSchema),
  z.lazy(() => LinkedinSearchCell$inboundSchema),
  z.lazy(() => ListAppsCell$inboundSchema),
  z.lazy(() => ListDashboardsCell$inboundSchema),
  z.lazy(() => ListUsersCell$inboundSchema),
  z.lazy(() => McpToolCell$inboundSchema),
  z.lazy(() => MdCell$inboundSchema),
  z.lazy(() => MetricsCell$inboundSchema),
  z.lazy(() => Microsoft365CalendarCell$inboundSchema),
  z.lazy(() => Microsoft365EmailContentCell$inboundSchema),
  z.lazy(() => Microsoft365EmailSearchCell$inboundSchema),
  z.lazy(() => OntologyEditorCell$inboundSchema),
  z.lazy(() => OntologyOpenObjectCell$inboundSchema),
  z.lazy(() => OntologyQueryCell$inboundSchema),
  z.lazy(() => OntologySearchMetricsCell$inboundSchema),
  z.lazy(() => PatchCell$inboundSchema),
  z.lazy(() => PlaybookEditorCell$inboundSchema),
  z.lazy(() => PowerbiCell$inboundSchema),
  z.lazy(() => PowerbiDaxCell$inboundSchema),
  z.lazy(() => PreviewCell$inboundSchema),
  z.lazy(() => PyCell$inboundSchema),
  z.lazy(() => QuestionsCell$inboundSchema),
  z.lazy(() => ReportCell$inboundSchema),
  z.lazy(() => ReportHistoryCell$inboundSchema),
  z.lazy(() => SqlCell$inboundSchema),
  z.lazy(() => StatusCell$inboundSchema),
  z.lazy(() => StreamlitCell$inboundSchema),
  z.lazy(() => SummaryCell$inboundSchema),
  z.lazy(() => TableauCell$inboundSchema),
  z.lazy(() => TableauSearchFieldsCell$inboundSchema),
  z.lazy(() => TableauSqlCell$inboundSchema),
  z.lazy(() => TabularFileCell$inboundSchema),
  z.lazy(() => TextCell$inboundSchema),
  z.lazy(() => UseSkillCell$inboundSchema),
  z.lazy(() => WsCell$inboundSchema),
]);

export function textqlRpcPublicChatCellFromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatCell, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TextqlRpcPublicChatCell$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatCell' from JSON`,
  );
}
