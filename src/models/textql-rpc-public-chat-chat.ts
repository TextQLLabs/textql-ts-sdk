/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  TextqlRpcPublicChatChatSource,
  TextqlRpcPublicChatChatSource$inboundSchema,
} from "./textql-rpc-public-chat-chat-source.js";
import {
  TextqlRpcPublicChatLlmModel,
  TextqlRpcPublicChatLlmModel$inboundSchema,
} from "./textql-rpc-public-chat-llm-model.js";
import {
  TextqlRpcPublicChatMethodology,
  TextqlRpcPublicChatMethodology$inboundSchema,
} from "./textql-rpc-public-chat-methodology.js";
import {
  TextqlRpcPublicParadigmParadigm,
  TextqlRpcPublicParadigmParadigm$inboundSchema,
} from "./textql-rpc-public-paradigm-paradigm.js";

export type TextqlRpcPublicChatChat = {
  /**
   * UUID
   */
  id?: string | undefined;
  /**
   * ChatParadigm includes paradigm options
   */
  paradigm?: TextqlRpcPublicParadigmParadigm | undefined;
  model?: TextqlRpcPublicChatLlmModel | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  timestamp?: Date | undefined;
  orgId?: string | undefined;
  memberId?: string | undefined;
  summary?: string | null | undefined;
  playbookId?: string | null | undefined;
  /**
   * whether this chat was created with report mode enabled
   */
  research?: boolean | null | undefined;
  creatorEmail?: string | null | undefined;
  apiKeyClientId?: string | null | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  updatedAt?: Date | undefined;
  /**
   * whether this chat is bookmarked by the user
   */
  isBookmarked?: boolean | null | undefined;
  preferredProvider?: string | null | undefined;
  /**
   * UUID reference to playbook_template_data
   */
  templateDataId?: string | null | undefined;
  /**
   * UUID reference to playbook_batch_run
   */
  batchRunId?: string | null | undefined;
  /**
   * preview/snippet of first message content
   */
  preview?: string | null | undefined;
  /**
   * whether this chat was created with dashboard mode enabled
   */
  dashboardMode?: boolean | null | undefined;
  source?: TextqlRpcPublicChatChatSource | undefined;
  methodology?: TextqlRpcPublicChatMethodology | undefined;
  /**
   * whether this chat has an active stream
   */
  isRunning?: boolean | null | undefined;
  /**
   * whether updated_at > last_read_at for the requesting user
   */
  isUnread?: boolean | null | undefined;
  /**
   * vllm_model_id is the per-chat model identifier for the org's vLLM endpoint.
   *
   * @remarks
   *  Only meaningful when model == MODEL_VLLM.
   */
  vllmModelId?: string | null | undefined;
  /**
   * fast_mode enables Anthropic's fast inference mode (speed: "fast").
   *
   * @remarks
   *  Currently supported on Opus 4.6 only. Pricing is 6x standard rates.
   */
  fastMode?: boolean | null | undefined;
  /**
   * Agent that owns this chat, unset for human-initiated chats.
   */
  agentId?: string | null | undefined;
  agentName?: string | null | undefined;
  agentProfileImageUrl?: string | null | undefined;
  /**
   * max_thinking runs extended thinking at max effort with visible reasoning.
   *
   * @remarks
   *  Supported on Sonnet 5, Fable 5, and Opus 4.8. Thinking tokens bill as output tokens.
   */
  maxThinking?: boolean | null | undefined;
};

/** @internal */
export const TextqlRpcPublicChatChat$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicChatChat,
  unknown
> = z.object({
  id: types.optional(types.string()),
  paradigm: types.optional(TextqlRpcPublicParadigmParadigm$inboundSchema),
  model: types.optional(TextqlRpcPublicChatLlmModel$inboundSchema),
  timestamp: types.optional(types.date()),
  orgId: types.optional(types.string()),
  memberId: types.optional(types.string()),
  summary: z.optional(z.nullable(types.string())),
  playbookId: z.optional(z.nullable(types.string())),
  research: z.optional(z.nullable(types.boolean())),
  creatorEmail: z.optional(z.nullable(types.string())),
  apiKeyClientId: z.optional(z.nullable(types.string())),
  updatedAt: types.optional(types.date()),
  isBookmarked: z.optional(z.nullable(types.boolean())),
  preferredProvider: z.optional(z.nullable(types.string())),
  templateDataId: z.optional(z.nullable(types.string())),
  batchRunId: z.optional(z.nullable(types.string())),
  preview: z.optional(z.nullable(types.string())),
  dashboardMode: z.optional(z.nullable(types.boolean())),
  source: types.optional(TextqlRpcPublicChatChatSource$inboundSchema),
  methodology: types.optional(TextqlRpcPublicChatMethodology$inboundSchema),
  isRunning: z.optional(z.nullable(types.boolean())),
  isUnread: z.optional(z.nullable(types.boolean())),
  vllmModelId: z.optional(z.nullable(types.string())),
  fastMode: z.optional(z.nullable(types.boolean())),
  agentId: z.optional(z.nullable(types.string())),
  agentName: z.optional(z.nullable(types.string())),
  agentProfileImageUrl: z.optional(z.nullable(types.string())),
  maxThinking: z.optional(z.nullable(types.boolean())),
});

export function textqlRpcPublicChatChatFromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicChatChat, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TextqlRpcPublicChatChat$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicChatChat' from JSON`,
  );
}
