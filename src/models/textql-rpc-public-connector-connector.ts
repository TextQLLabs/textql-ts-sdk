/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { smartUnion } from "../types/smart-union.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  TextqlRpcPublicConnectorAthenaMetadata,
  TextqlRpcPublicConnectorAthenaMetadata$inboundSchema,
} from "./textql-rpc-public-connector-athena-metadata.js";
import {
  TextqlRpcPublicConnectorAuroraMetadata,
  TextqlRpcPublicConnectorAuroraMetadata$inboundSchema,
} from "./textql-rpc-public-connector-aurora-metadata.js";
import {
  TextqlRpcPublicConnectorAzureSynapseMetadata,
  TextqlRpcPublicConnectorAzureSynapseMetadata$inboundSchema,
} from "./textql-rpc-public-connector-azure-synapse-metadata.js";
import {
  TextqlRpcPublicConnectorBigQueryMetadata,
  TextqlRpcPublicConnectorBigQueryMetadata$inboundSchema,
} from "./textql-rpc-public-connector-big-query-metadata.js";
import {
  TextqlRpcPublicConnectorClickHouseMetadata,
  TextqlRpcPublicConnectorClickHouseMetadata$inboundSchema,
} from "./textql-rpc-public-connector-click-house-metadata.js";
import {
  TextqlRpcPublicConnectorConnectorType,
  TextqlRpcPublicConnectorConnectorType$inboundSchema,
} from "./textql-rpc-public-connector-connector-type.js";
import {
  TextqlRpcPublicConnectorDatabricksMetadata,
  TextqlRpcPublicConnectorDatabricksMetadata$inboundSchema,
} from "./textql-rpc-public-connector-databricks-metadata.js";
import {
  TextqlRpcPublicConnectorDremioMetadata,
  TextqlRpcPublicConnectorDremioMetadata$inboundSchema,
} from "./textql-rpc-public-connector-dremio-metadata.js";
import {
  TextqlRpcPublicConnectorExasolMetadata,
  TextqlRpcPublicConnectorExasolMetadata$inboundSchema,
} from "./textql-rpc-public-connector-exasol-metadata.js";
import {
  TextqlRpcPublicConnectorFireboltMetadata,
  TextqlRpcPublicConnectorFireboltMetadata$inboundSchema,
} from "./textql-rpc-public-connector-firebolt-metadata.js";
import {
  TextqlRpcPublicConnectorGmailMetadata,
  TextqlRpcPublicConnectorGmailMetadata$inboundSchema,
} from "./textql-rpc-public-connector-gmail-metadata.js";
import {
  TextqlRpcPublicConnectorGoogleCalendarMetadata,
  TextqlRpcPublicConnectorGoogleCalendarMetadata$inboundSchema,
} from "./textql-rpc-public-connector-google-calendar-metadata.js";
import {
  TextqlRpcPublicConnectorGoogleDriveMetadata,
  TextqlRpcPublicConnectorGoogleDriveMetadata$inboundSchema,
} from "./textql-rpc-public-connector-google-drive-metadata.js";
import {
  TextqlRpcPublicConnectorGoogleMetadata,
  TextqlRpcPublicConnectorGoogleMetadata$inboundSchema,
} from "./textql-rpc-public-connector-google-metadata.js";
import {
  TextqlRpcPublicConnectorKdbMetadata,
  TextqlRpcPublicConnectorKdbMetadata$inboundSchema,
} from "./textql-rpc-public-connector-kdb-metadata.js";
import {
  TextqlRpcPublicConnectorMicrosoft365Metadata,
  TextqlRpcPublicConnectorMicrosoft365Metadata$inboundSchema,
} from "./textql-rpc-public-connector-microsoft365-metadata.js";
import {
  TextqlRpcPublicConnectorMongoDBMetadata,
  TextqlRpcPublicConnectorMongoDBMetadata$inboundSchema,
} from "./textql-rpc-public-connector-mongo-db-metadata.js";
import {
  TextqlRpcPublicConnectorMotherduckMetadata,
  TextqlRpcPublicConnectorMotherduckMetadata$inboundSchema,
} from "./textql-rpc-public-connector-motherduck-metadata.js";
import {
  TextqlRpcPublicConnectorMYSQLMetadata,
  TextqlRpcPublicConnectorMYSQLMetadata$inboundSchema,
} from "./textql-rpc-public-connector-mysql-metadata.js";
import {
  TextqlRpcPublicConnectorOracleMetadata,
  TextqlRpcPublicConnectorOracleMetadata$inboundSchema,
} from "./textql-rpc-public-connector-oracle-metadata.js";
import {
  TextqlRpcPublicConnectorPostgresMetadata,
  TextqlRpcPublicConnectorPostgresMetadata$inboundSchema,
} from "./textql-rpc-public-connector-postgres-metadata.js";
import {
  TextqlRpcPublicConnectorPowerBIMetadata,
  TextqlRpcPublicConnectorPowerBIMetadata$inboundSchema,
} from "./textql-rpc-public-connector-power-bi-metadata.js";
import {
  TextqlRpcPublicConnectorRedshiftMetadata,
  TextqlRpcPublicConnectorRedshiftMetadata$inboundSchema,
} from "./textql-rpc-public-connector-redshift-metadata.js";
import {
  TextqlRpcPublicConnectorSAPHanaMetadata,
  TextqlRpcPublicConnectorSAPHanaMetadata$inboundSchema,
} from "./textql-rpc-public-connector-sap-hana-metadata.js";
import {
  TextqlRpcPublicConnectorSnowflakeMetadata,
  TextqlRpcPublicConnectorSnowflakeMetadata$inboundSchema,
} from "./textql-rpc-public-connector-snowflake-metadata.js";
import {
  TextqlRpcPublicConnectorSQLServerMetadata,
  TextqlRpcPublicConnectorSQLServerMetadata$inboundSchema,
} from "./textql-rpc-public-connector-sql-server-metadata.js";
import {
  TextqlRpcPublicConnectorSupabaseMetadata,
  TextqlRpcPublicConnectorSupabaseMetadata$inboundSchema,
} from "./textql-rpc-public-connector-supabase-metadata.js";
import {
  TextqlRpcPublicConnectorTableauMetadata,
  TextqlRpcPublicConnectorTableauMetadata$inboundSchema,
} from "./textql-rpc-public-connector-tableau-metadata.js";
import {
  TextqlRpcPublicConnectorTrinoMetadata,
  TextqlRpcPublicConnectorTrinoMetadata$inboundSchema,
} from "./textql-rpc-public-connector-trino-metadata.js";

export type TrinoMetadata = {
  trinoMetadata: TextqlRpcPublicConnectorTrinoMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type TableauMetadata = {
  tableauMetadata: TextqlRpcPublicConnectorTableauMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type SupabaseMetadata = {
  supabaseMetadata: TextqlRpcPublicConnectorSupabaseMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type SqlServerMetadata = {
  sqlServerMetadata: TextqlRpcPublicConnectorSQLServerMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type SnowflakeMetadata = {
  snowflakeMetadata: TextqlRpcPublicConnectorSnowflakeMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type SapHanaMetadata = {
  sapHanaMetadata: TextqlRpcPublicConnectorSAPHanaMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type RedshiftMetadata = {
  redshiftMetadata: TextqlRpcPublicConnectorRedshiftMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type PowerbiMetadata = {
  powerbiMetadata: TextqlRpcPublicConnectorPowerBIMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type PostgresMetadata = {
  postgresMetadata: TextqlRpcPublicConnectorPostgresMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type OracleMetadata = {
  oracleMetadata: TextqlRpcPublicConnectorOracleMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type MysqlMetadata = {
  mysqlMetadata: TextqlRpcPublicConnectorMYSQLMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type MotherduckMetadata = {
  motherduckMetadata: TextqlRpcPublicConnectorMotherduckMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type MongodbMetadata = {
  mongodbMetadata: TextqlRpcPublicConnectorMongoDBMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type Microsoft365Metadata = {
  microsoft365Metadata: TextqlRpcPublicConnectorMicrosoft365Metadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type KdbMetadata = {
  /**
   * KdbMetadata configures a kdb+ (kx/q) connector. kdb+ speaks its own binary IPC
   *
   * @remarks
   *  protocol (not SQL), so queries are qSQL strings; see pkg/connectors/kdbipc.
   */
  kdbMetadata: TextqlRpcPublicConnectorKdbMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type GoogleMetadata = {
  googleMetadata: TextqlRpcPublicConnectorGoogleMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type GoogleDriveMetadata = {
  googleDriveMetadata: TextqlRpcPublicConnectorGoogleDriveMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type GoogleCalendarMetadata = {
  googleCalendarMetadata: TextqlRpcPublicConnectorGoogleCalendarMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type GmailMetadata = {
  gmailMetadata: TextqlRpcPublicConnectorGmailMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type FireboltMetadata = {
  fireboltMetadata: TextqlRpcPublicConnectorFireboltMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type ExasolMetadata = {
  exasolMetadata: TextqlRpcPublicConnectorExasolMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type DremioMetadata = {
  dremioMetadata: TextqlRpcPublicConnectorDremioMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type DatabricksMetadata = {
  databricksMetadata: TextqlRpcPublicConnectorDatabricksMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type ClickhouseMetadata = {
  clickhouseMetadata: TextqlRpcPublicConnectorClickHouseMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type BigqueryMetadata = {
  bigqueryMetadata: TextqlRpcPublicConnectorBigQueryMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type AzureSynapseMetadata = {
  azureSynapseMetadata: TextqlRpcPublicConnectorAzureSynapseMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type AuroraMetadata = {
  auroraMetadata: TextqlRpcPublicConnectorAuroraMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type AthenaMetadata = {
  athenaMetadata: TextqlRpcPublicConnectorAthenaMetadata;
  id?: number | undefined;
  name?: string | undefined;
  connectorType?: TextqlRpcPublicConnectorConnectorType | undefined;
  memberId?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  isExample?: boolean | undefined;
  allowSqlWriteOperations?: boolean | undefined;
  authStrategy?: string | undefined;
  authenticatedByMemberId?: string | undefined;
  /**
   * Per-member auth status for the requesting user (only set for per_member_oauth connectors)
   */
  memberAuthenticated?: boolean | null | undefined;
  memberAuthUsername?: string | null | undefined;
  includeDbSessionMetadata?: boolean | undefined;
  /**
   * Org-visible (db.ConnectorIsPublic): a public access row, or no access rows.
   */
  isPublic?: boolean | undefined;
};

export type TextqlRpcPublicConnectorConnector =
  | AthenaMetadata
  | AuroraMetadata
  | AzureSynapseMetadata
  | BigqueryMetadata
  | ClickhouseMetadata
  | DatabricksMetadata
  | DremioMetadata
  | ExasolMetadata
  | FireboltMetadata
  | GmailMetadata
  | GoogleCalendarMetadata
  | GoogleDriveMetadata
  | GoogleMetadata
  | KdbMetadata
  | Microsoft365Metadata
  | MongodbMetadata
  | MotherduckMetadata
  | MysqlMetadata
  | OracleMetadata
  | PostgresMetadata
  | PowerbiMetadata
  | RedshiftMetadata
  | SapHanaMetadata
  | SnowflakeMetadata
  | SqlServerMetadata
  | SupabaseMetadata
  | TableauMetadata
  | TrinoMetadata;

/** @internal */
export const TrinoMetadata$inboundSchema: z.ZodMiniType<
  TrinoMetadata,
  unknown
> = z.object({
  trinoMetadata: TextqlRpcPublicConnectorTrinoMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function trinoMetadataFromJSON(
  jsonString: string,
): SafeParseResult<TrinoMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TrinoMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TrinoMetadata' from JSON`,
  );
}

/** @internal */
export const TableauMetadata$inboundSchema: z.ZodMiniType<
  TableauMetadata,
  unknown
> = z.object({
  tableauMetadata: TextqlRpcPublicConnectorTableauMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function tableauMetadataFromJSON(
  jsonString: string,
): SafeParseResult<TableauMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TableauMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TableauMetadata' from JSON`,
  );
}

/** @internal */
export const SupabaseMetadata$inboundSchema: z.ZodMiniType<
  SupabaseMetadata,
  unknown
> = z.object({
  supabaseMetadata: TextqlRpcPublicConnectorSupabaseMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function supabaseMetadataFromJSON(
  jsonString: string,
): SafeParseResult<SupabaseMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SupabaseMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SupabaseMetadata' from JSON`,
  );
}

/** @internal */
export const SqlServerMetadata$inboundSchema: z.ZodMiniType<
  SqlServerMetadata,
  unknown
> = z.object({
  sqlServerMetadata: TextqlRpcPublicConnectorSQLServerMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function sqlServerMetadataFromJSON(
  jsonString: string,
): SafeParseResult<SqlServerMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SqlServerMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SqlServerMetadata' from JSON`,
  );
}

/** @internal */
export const SnowflakeMetadata$inboundSchema: z.ZodMiniType<
  SnowflakeMetadata,
  unknown
> = z.object({
  snowflakeMetadata: TextqlRpcPublicConnectorSnowflakeMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function snowflakeMetadataFromJSON(
  jsonString: string,
): SafeParseResult<SnowflakeMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SnowflakeMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SnowflakeMetadata' from JSON`,
  );
}

/** @internal */
export const SapHanaMetadata$inboundSchema: z.ZodMiniType<
  SapHanaMetadata,
  unknown
> = z.object({
  sapHanaMetadata: TextqlRpcPublicConnectorSAPHanaMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function sapHanaMetadataFromJSON(
  jsonString: string,
): SafeParseResult<SapHanaMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SapHanaMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SapHanaMetadata' from JSON`,
  );
}

/** @internal */
export const RedshiftMetadata$inboundSchema: z.ZodMiniType<
  RedshiftMetadata,
  unknown
> = z.object({
  redshiftMetadata: TextqlRpcPublicConnectorRedshiftMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function redshiftMetadataFromJSON(
  jsonString: string,
): SafeParseResult<RedshiftMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RedshiftMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RedshiftMetadata' from JSON`,
  );
}

/** @internal */
export const PowerbiMetadata$inboundSchema: z.ZodMiniType<
  PowerbiMetadata,
  unknown
> = z.object({
  powerbiMetadata: TextqlRpcPublicConnectorPowerBIMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function powerbiMetadataFromJSON(
  jsonString: string,
): SafeParseResult<PowerbiMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PowerbiMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PowerbiMetadata' from JSON`,
  );
}

/** @internal */
export const PostgresMetadata$inboundSchema: z.ZodMiniType<
  PostgresMetadata,
  unknown
> = z.object({
  postgresMetadata: TextqlRpcPublicConnectorPostgresMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function postgresMetadataFromJSON(
  jsonString: string,
): SafeParseResult<PostgresMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PostgresMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PostgresMetadata' from JSON`,
  );
}

/** @internal */
export const OracleMetadata$inboundSchema: z.ZodMiniType<
  OracleMetadata,
  unknown
> = z.object({
  oracleMetadata: TextqlRpcPublicConnectorOracleMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function oracleMetadataFromJSON(
  jsonString: string,
): SafeParseResult<OracleMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OracleMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OracleMetadata' from JSON`,
  );
}

/** @internal */
export const MysqlMetadata$inboundSchema: z.ZodMiniType<
  MysqlMetadata,
  unknown
> = z.object({
  mysqlMetadata: TextqlRpcPublicConnectorMYSQLMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function mysqlMetadataFromJSON(
  jsonString: string,
): SafeParseResult<MysqlMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MysqlMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MysqlMetadata' from JSON`,
  );
}

/** @internal */
export const MotherduckMetadata$inboundSchema: z.ZodMiniType<
  MotherduckMetadata,
  unknown
> = z.object({
  motherduckMetadata: TextqlRpcPublicConnectorMotherduckMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function motherduckMetadataFromJSON(
  jsonString: string,
): SafeParseResult<MotherduckMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MotherduckMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MotherduckMetadata' from JSON`,
  );
}

/** @internal */
export const MongodbMetadata$inboundSchema: z.ZodMiniType<
  MongodbMetadata,
  unknown
> = z.object({
  mongodbMetadata: TextqlRpcPublicConnectorMongoDBMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function mongodbMetadataFromJSON(
  jsonString: string,
): SafeParseResult<MongodbMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MongodbMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MongodbMetadata' from JSON`,
  );
}

/** @internal */
export const Microsoft365Metadata$inboundSchema: z.ZodMiniType<
  Microsoft365Metadata,
  unknown
> = z.object({
  microsoft365Metadata:
    TextqlRpcPublicConnectorMicrosoft365Metadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function microsoft365MetadataFromJSON(
  jsonString: string,
): SafeParseResult<Microsoft365Metadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Microsoft365Metadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Microsoft365Metadata' from JSON`,
  );
}

/** @internal */
export const KdbMetadata$inboundSchema: z.ZodMiniType<KdbMetadata, unknown> = z
  .object({
    kdbMetadata: TextqlRpcPublicConnectorKdbMetadata$inboundSchema,
    id: types.optional(types.number()),
    name: types.optional(types.string()),
    connectorType: types.optional(
      TextqlRpcPublicConnectorConnectorType$inboundSchema,
    ),
    memberId: types.optional(types.string()),
    createdAt: types.optional(types.date()),
    isExample: types.optional(types.boolean()),
    allowSqlWriteOperations: types.optional(types.boolean()),
    authStrategy: types.optional(types.string()),
    authenticatedByMemberId: types.optional(types.string()),
    memberAuthenticated: z.optional(z.nullable(types.boolean())),
    memberAuthUsername: z.optional(z.nullable(types.string())),
    includeDbSessionMetadata: types.optional(types.boolean()),
    isPublic: types.optional(types.boolean()),
  });

export function kdbMetadataFromJSON(
  jsonString: string,
): SafeParseResult<KdbMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => KdbMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'KdbMetadata' from JSON`,
  );
}

/** @internal */
export const GoogleMetadata$inboundSchema: z.ZodMiniType<
  GoogleMetadata,
  unknown
> = z.object({
  googleMetadata: TextqlRpcPublicConnectorGoogleMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function googleMetadataFromJSON(
  jsonString: string,
): SafeParseResult<GoogleMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GoogleMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GoogleMetadata' from JSON`,
  );
}

/** @internal */
export const GoogleDriveMetadata$inboundSchema: z.ZodMiniType<
  GoogleDriveMetadata,
  unknown
> = z.object({
  googleDriveMetadata:
    TextqlRpcPublicConnectorGoogleDriveMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function googleDriveMetadataFromJSON(
  jsonString: string,
): SafeParseResult<GoogleDriveMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GoogleDriveMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GoogleDriveMetadata' from JSON`,
  );
}

/** @internal */
export const GoogleCalendarMetadata$inboundSchema: z.ZodMiniType<
  GoogleCalendarMetadata,
  unknown
> = z.object({
  googleCalendarMetadata:
    TextqlRpcPublicConnectorGoogleCalendarMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function googleCalendarMetadataFromJSON(
  jsonString: string,
): SafeParseResult<GoogleCalendarMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GoogleCalendarMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GoogleCalendarMetadata' from JSON`,
  );
}

/** @internal */
export const GmailMetadata$inboundSchema: z.ZodMiniType<
  GmailMetadata,
  unknown
> = z.object({
  gmailMetadata: TextqlRpcPublicConnectorGmailMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function gmailMetadataFromJSON(
  jsonString: string,
): SafeParseResult<GmailMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GmailMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GmailMetadata' from JSON`,
  );
}

/** @internal */
export const FireboltMetadata$inboundSchema: z.ZodMiniType<
  FireboltMetadata,
  unknown
> = z.object({
  fireboltMetadata: TextqlRpcPublicConnectorFireboltMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function fireboltMetadataFromJSON(
  jsonString: string,
): SafeParseResult<FireboltMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FireboltMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FireboltMetadata' from JSON`,
  );
}

/** @internal */
export const ExasolMetadata$inboundSchema: z.ZodMiniType<
  ExasolMetadata,
  unknown
> = z.object({
  exasolMetadata: TextqlRpcPublicConnectorExasolMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function exasolMetadataFromJSON(
  jsonString: string,
): SafeParseResult<ExasolMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExasolMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExasolMetadata' from JSON`,
  );
}

/** @internal */
export const DremioMetadata$inboundSchema: z.ZodMiniType<
  DremioMetadata,
  unknown
> = z.object({
  dremioMetadata: TextqlRpcPublicConnectorDremioMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function dremioMetadataFromJSON(
  jsonString: string,
): SafeParseResult<DremioMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DremioMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DremioMetadata' from JSON`,
  );
}

/** @internal */
export const DatabricksMetadata$inboundSchema: z.ZodMiniType<
  DatabricksMetadata,
  unknown
> = z.object({
  databricksMetadata: TextqlRpcPublicConnectorDatabricksMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function databricksMetadataFromJSON(
  jsonString: string,
): SafeParseResult<DatabricksMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DatabricksMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DatabricksMetadata' from JSON`,
  );
}

/** @internal */
export const ClickhouseMetadata$inboundSchema: z.ZodMiniType<
  ClickhouseMetadata,
  unknown
> = z.object({
  clickhouseMetadata: TextqlRpcPublicConnectorClickHouseMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function clickhouseMetadataFromJSON(
  jsonString: string,
): SafeParseResult<ClickhouseMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ClickhouseMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ClickhouseMetadata' from JSON`,
  );
}

/** @internal */
export const BigqueryMetadata$inboundSchema: z.ZodMiniType<
  BigqueryMetadata,
  unknown
> = z.object({
  bigqueryMetadata: TextqlRpcPublicConnectorBigQueryMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function bigqueryMetadataFromJSON(
  jsonString: string,
): SafeParseResult<BigqueryMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BigqueryMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BigqueryMetadata' from JSON`,
  );
}

/** @internal */
export const AzureSynapseMetadata$inboundSchema: z.ZodMiniType<
  AzureSynapseMetadata,
  unknown
> = z.object({
  azureSynapseMetadata:
    TextqlRpcPublicConnectorAzureSynapseMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function azureSynapseMetadataFromJSON(
  jsonString: string,
): SafeParseResult<AzureSynapseMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AzureSynapseMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AzureSynapseMetadata' from JSON`,
  );
}

/** @internal */
export const AuroraMetadata$inboundSchema: z.ZodMiniType<
  AuroraMetadata,
  unknown
> = z.object({
  auroraMetadata: TextqlRpcPublicConnectorAuroraMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function auroraMetadataFromJSON(
  jsonString: string,
): SafeParseResult<AuroraMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AuroraMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AuroraMetadata' from JSON`,
  );
}

/** @internal */
export const AthenaMetadata$inboundSchema: z.ZodMiniType<
  AthenaMetadata,
  unknown
> = z.object({
  athenaMetadata: TextqlRpcPublicConnectorAthenaMetadata$inboundSchema,
  id: types.optional(types.number()),
  name: types.optional(types.string()),
  connectorType: types.optional(
    TextqlRpcPublicConnectorConnectorType$inboundSchema,
  ),
  memberId: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  isExample: types.optional(types.boolean()),
  allowSqlWriteOperations: types.optional(types.boolean()),
  authStrategy: types.optional(types.string()),
  authenticatedByMemberId: types.optional(types.string()),
  memberAuthenticated: z.optional(z.nullable(types.boolean())),
  memberAuthUsername: z.optional(z.nullable(types.string())),
  includeDbSessionMetadata: types.optional(types.boolean()),
  isPublic: types.optional(types.boolean()),
});

export function athenaMetadataFromJSON(
  jsonString: string,
): SafeParseResult<AthenaMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AthenaMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AthenaMetadata' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicConnectorConnector$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicConnectorConnector,
  unknown
> = smartUnion([
  z.lazy(() => AthenaMetadata$inboundSchema),
  z.lazy(() => AuroraMetadata$inboundSchema),
  z.lazy(() => AzureSynapseMetadata$inboundSchema),
  z.lazy(() => BigqueryMetadata$inboundSchema),
  z.lazy(() => ClickhouseMetadata$inboundSchema),
  z.lazy(() => DatabricksMetadata$inboundSchema),
  z.lazy(() => DremioMetadata$inboundSchema),
  z.lazy(() => ExasolMetadata$inboundSchema),
  z.lazy(() => FireboltMetadata$inboundSchema),
  z.lazy(() => GmailMetadata$inboundSchema),
  z.lazy(() => GoogleCalendarMetadata$inboundSchema),
  z.lazy(() => GoogleDriveMetadata$inboundSchema),
  z.lazy(() => GoogleMetadata$inboundSchema),
  z.lazy(() => KdbMetadata$inboundSchema),
  z.lazy(() => Microsoft365Metadata$inboundSchema),
  z.lazy(() => MongodbMetadata$inboundSchema),
  z.lazy(() => MotherduckMetadata$inboundSchema),
  z.lazy(() => MysqlMetadata$inboundSchema),
  z.lazy(() => OracleMetadata$inboundSchema),
  z.lazy(() => PostgresMetadata$inboundSchema),
  z.lazy(() => PowerbiMetadata$inboundSchema),
  z.lazy(() => RedshiftMetadata$inboundSchema),
  z.lazy(() => SapHanaMetadata$inboundSchema),
  z.lazy(() => SnowflakeMetadata$inboundSchema),
  z.lazy(() => SqlServerMetadata$inboundSchema),
  z.lazy(() => SupabaseMetadata$inboundSchema),
  z.lazy(() => TableauMetadata$inboundSchema),
  z.lazy(() => TrinoMetadata$inboundSchema),
]);

export function textqlRpcPublicConnectorConnectorFromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicConnectorConnector, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TextqlRpcPublicConnectorConnector$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicConnectorConnector' from JSON`,
  );
}
