/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { smartUnion } from "../types/smart-union.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * estimated tokens held by dead files (~size/4)
 */
export type ReclaimableTokens = number | string;

/**
 * Aggregate ontology-usage health for the window — the roll-ups the Ontology
 *
 * @remarks
 *  Health hero needs without paging every file to the client. pulled_files,
 *  avg_hit_rate, and error_files are Postgres aggregates over the pull/run data;
 *  total_files, dead_files, and reclaimable_tokens come from the current git
 *  tree diffed against the set of pulled paths (a dead file is one present in
 *  the ontology but never pulled in the window).
 */
export type TextqlRpcPublicPatchesGetOntologyUsageSummaryResponse = {
  totalFiles?: number | undefined;
  pulledFiles?: number | undefined;
  deadFiles?: number | undefined;
  /**
   * 0..1, averaged over pulled files
   */
  avgHitRate?: number | undefined;
  /**
   * files with at least one errored pull in the window
   */
  errorFiles?: number | undefined;
  /**
   * estimated tokens held by dead files (~size/4)
   */
  reclaimableTokens?: number | string | undefined;
};

/** @internal */
export const ReclaimableTokens$inboundSchema: z.ZodMiniType<
  ReclaimableTokens,
  unknown
> = smartUnion([types.number(), types.string()]);

export function reclaimableTokensFromJSON(
  jsonString: string,
): SafeParseResult<ReclaimableTokens, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReclaimableTokens$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReclaimableTokens' from JSON`,
  );
}

/** @internal */
export const TextqlRpcPublicPatchesGetOntologyUsageSummaryResponse$inboundSchema:
  z.ZodMiniType<
    TextqlRpcPublicPatchesGetOntologyUsageSummaryResponse,
    unknown
  > = z.object({
    totalFiles: types.optional(types.number()),
    pulledFiles: types.optional(types.number()),
    deadFiles: types.optional(types.number()),
    avgHitRate: types.optional(types.number()),
    errorFiles: types.optional(types.number()),
    reclaimableTokens: types.optional(
      smartUnion([types.number(), types.string()]),
    ),
  });

export function textqlRpcPublicPatchesGetOntologyUsageSummaryResponseFromJSON(
  jsonString: string,
): SafeParseResult<
  TextqlRpcPublicPatchesGetOntologyUsageSummaryResponse,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      TextqlRpcPublicPatchesGetOntologyUsageSummaryResponse$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'TextqlRpcPublicPatchesGetOntologyUsageSummaryResponse' from JSON`,
  );
}
