/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  TextqlRpcIdentityMemberPreview,
  TextqlRpcIdentityMemberPreview$inboundSchema,
} from "./textql-rpc-identity-member-preview.js";
import {
  TextqlRpcParadigmParamsParadigmType,
  TextqlRpcParadigmParamsParadigmType$inboundSchema,
} from "./textql-rpc-paradigm-params-paradigm-type.js";
import {
  TextqlRpcPublicChatLlmModel,
  TextqlRpcPublicChatLlmModel$inboundSchema,
} from "./textql-rpc-public-chat-llm-model.js";
import {
  TextqlRpcPublicConfigSourceConfigSource,
  TextqlRpcPublicConfigSourceConfigSource$inboundSchema,
} from "./textql-rpc-public-config-source-config-source.js";
import {
  TextqlRpcPublicParadigmParadigmOptions,
  TextqlRpcPublicParadigmParadigmOptions$inboundSchema,
} from "./textql-rpc-public-paradigm-paradigm-options.js";
import {
  TextqlRpcPublicPlaybookChatAccessMode,
  TextqlRpcPublicPlaybookChatAccessMode$inboundSchema,
} from "./textql-rpc-public-playbook-chat-access-mode.js";
import {
  TextqlRpcPublicPlaybookChatAccessOverride,
  TextqlRpcPublicPlaybookChatAccessOverride$inboundSchema,
} from "./textql-rpc-public-playbook-chat-access-override.js";
import {
  TextqlRpcPublicPlaybookFilterCondition,
  TextqlRpcPublicPlaybookFilterCondition$inboundSchema,
} from "./textql-rpc-public-playbook-filter-condition.js";
import {
  TextqlRpcPublicPlaybookPlaybookReportStyle,
  TextqlRpcPublicPlaybookPlaybookReportStyle$inboundSchema,
} from "./textql-rpc-public-playbook-playbook-report-style.js";
import {
  TextqlRpcPublicPlaybookPlaybookStatus,
  TextqlRpcPublicPlaybookPlaybookStatus$inboundSchema,
} from "./textql-rpc-public-playbook-playbook-status.js";
import {
  TextqlRpcPublicPlaybookPlaybookTriggerType,
  TextqlRpcPublicPlaybookPlaybookTriggerType$inboundSchema,
} from "./textql-rpc-public-playbook-playbook-trigger-type.js";

export type TextqlRpcPublicPlaybookPlaybook = {
  /**
   * UUID
   */
  id?: string | undefined;
  orgId?: string | undefined;
  memberId?: string | null | undefined;
  /**
   * Deprecated: use connector_ids instead
   *
   * @deprecated field: This will be removed in a future release, please migrate away from it as soon as possible.
   */
  connectorId?: number | null | undefined;
  name?: string | undefined;
  prompt?: string | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  createdAt?: Date | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  updatedAt?: Date | undefined;
  status?: TextqlRpcPublicPlaybookPlaybookStatus | undefined;
  triggerType?: TextqlRpcPublicPlaybookPlaybookTriggerType | undefined;
  cronString?: string | null | undefined;
  /**
   * List of UUIDs
   */
  datasetIds?: Array<string> | undefined;
  referenceReportId?: string | null | undefined;
  latestChatId?: string | null | undefined;
  emailAddresses?: Array<string> | undefined;
  slackChannelId?: string | null | undefined;
  taggedSlackUserIds?: Array<string> | undefined;
  llmModel?: TextqlRpcPublicChatLlmModel | undefined;
  paradigmOptions?: TextqlRpcPublicParadigmParadigmOptions | undefined;
  paradigmType?: TextqlRpcParadigmParamsParadigmType | undefined;
  isRunning?: boolean | null | undefined;
  owner?: TextqlRpcIdentityMemberPreview | undefined;
  hasWritePermission?: boolean | null | undefined;
  isSubscribed?: boolean | null | undefined;
  reportOutputStyle?: TextqlRpcPublicPlaybookPlaybookReportStyle | undefined;
  /**
   * UUID reference to PlaybookTemplateHeader
   */
  templateHeaderId?: string | null | undefined;
  /**
   * List of UUIDs for selected template data rows
   */
  selectedTemplateDataIds?: Array<string> | undefined;
  /**
   * Maximum number of templates to run concurrently
   */
  maxConcurrentTemplates?: number | null | undefined;
  /**
   * Auto-optimize concurrency based on runtime
   */
  autoOptimizeConcurrency?: boolean | null | undefined;
  connectorIds?: Array<number> | undefined;
  /**
   * Filter conditions for template data
   */
  templateDataFilters?:
    | Array<TextqlRpcPublicPlaybookFilterCondition>
    | undefined;
  parentPlaybookId?: string | null | undefined;
  originPlaybookId?: string | null | undefined;
  duplicatedByMemberId?: string | null | undefined;
  /**
   * A Timestamp represents a point in time independent of any time zone or local
   *
   * @remarks
   *  calendar, encoded as a count of seconds and fractions of seconds at
   *  nanosecond resolution. The count is relative to an epoch at UTC midnight on
   *  January 1, 1970, in the proleptic Gregorian calendar which extends the
   *  Gregorian calendar backwards to year one.
   *
   *  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
   *  second table is needed for interpretation, using a [24-hour linear
   *  smear](https://developers.google.com/time/smear).
   *
   *  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
   *  restricting to that range, we ensure that we can convert to and from [RFC
   *  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
   *
   *  # Examples
   *
   *  Example 1: Compute Timestamp from POSIX `time()`.
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(time(NULL));
   *      timestamp.set_nanos(0);
   *
   *  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
   *
   *      struct timeval tv;
   *      gettimeofday(&tv, NULL);
   *
   *      Timestamp timestamp;
   *      timestamp.set_seconds(tv.tv_sec);
   *      timestamp.set_nanos(tv.tv_usec * 1000);
   *
   *  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
   *
   *      FILETIME ft;
   *      GetSystemTimeAsFileTime(&ft);
   *      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
   *
   *      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
   *      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
   *      Timestamp timestamp;
   *      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
   *      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
   *
   *  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
   *
   *      long millis = System.currentTimeMillis();
   *
   *      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
   *          .setNanos((int) ((millis % 1000) * 1000000)).build();
   *
   *  Example 5: Compute Timestamp from Java `Instant.now()`.
   *
   *      Instant now = Instant.now();
   *
   *      Timestamp timestamp =
   *          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
   *              .setNanos(now.getNano()).build();
   *
   *  Example 6: Compute Timestamp from current time in Python.
   *
   *      timestamp = Timestamp()
   *      timestamp.GetCurrentTime()
   *
   *  # JSON Mapping
   *
   *  In JSON format, the Timestamp type is encoded as a string in the
   *  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
   *  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
   *  where {year} is always expressed using four digits while {month}, {day},
   *  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
   *  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
   *  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
   *  is required. A proto3 JSON serializer should always use UTC (as indicated by
   *  "Z") when printing the Timestamp type and a proto3 JSON parser should be
   *  able to accept both UTC and other timezones (as indicated by an offset).
   *
   *  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
   *  01:30 UTC on January 15, 2017.
   *
   *  In JavaScript, one can convert a Date object to this format using the
   *  standard
   *  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
   *  method. In Python, a standard `datetime.datetime` object can be converted
   *  to this format using
   *  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
   *  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
   *  the Joda Time's [`ISODateTimeFormat.dateTime()`](
   *  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
   *  ) to obtain a formatter capable of generating timestamps in this format.
   */
  duplicatedAt?: Date | undefined;
  chatAccessMode?: TextqlRpcPublicPlaybookChatAccessMode | undefined;
  chatAccessOverrides?:
    | Array<TextqlRpcPublicPlaybookChatAccessOverride>
    | undefined;
  chatIsPublic?: boolean | undefined;
  /**
   * List of dashboard UUIDs attached to this playbook
   */
  dashboardIds?: Array<string> | undefined;
  /**
   * Column name in template data to use as recipient email override
   */
  recipientEmailColumn?: string | null | undefined;
  teamsChannelId?: string | null | undefined;
  taggedTeamsUserAadIds?: Array<string> | undefined;
  /**
   * Config-management metadata (provenance + reconcile state) for any file-based
   *
   * @remarks
   *  object. Presence on an object ⇒ the object is config-managed.
   */
  configSource?: TextqlRpcPublicConfigSourceConfigSource | undefined;
  apiAccessKeyIds?: Array<string> | undefined;
};

/** @internal */
export const TextqlRpcPublicPlaybookPlaybook$inboundSchema: z.ZodMiniType<
  TextqlRpcPublicPlaybookPlaybook,
  unknown
> = z.object({
  id: types.optional(types.string()),
  orgId: types.optional(types.string()),
  memberId: z.optional(z.nullable(types.string())),
  connectorId: z.optional(z.nullable(types.number())),
  name: types.optional(types.string()),
  prompt: types.optional(types.string()),
  createdAt: types.optional(types.date()),
  updatedAt: types.optional(types.date()),
  status: types.optional(TextqlRpcPublicPlaybookPlaybookStatus$inboundSchema),
  triggerType: types.optional(
    TextqlRpcPublicPlaybookPlaybookTriggerType$inboundSchema,
  ),
  cronString: z.optional(z.nullable(types.string())),
  datasetIds: types.optional(z.array(types.string())),
  referenceReportId: z.optional(z.nullable(types.string())),
  latestChatId: z.optional(z.nullable(types.string())),
  emailAddresses: types.optional(z.array(types.string())),
  slackChannelId: z.optional(z.nullable(types.string())),
  taggedSlackUserIds: types.optional(z.array(types.string())),
  llmModel: types.optional(TextqlRpcPublicChatLlmModel$inboundSchema),
  paradigmOptions: types.optional(
    TextqlRpcPublicParadigmParadigmOptions$inboundSchema,
  ),
  paradigmType: types.optional(
    TextqlRpcParadigmParamsParadigmType$inboundSchema,
  ),
  isRunning: z.optional(z.nullable(types.boolean())),
  owner: types.optional(TextqlRpcIdentityMemberPreview$inboundSchema),
  hasWritePermission: z.optional(z.nullable(types.boolean())),
  isSubscribed: z.optional(z.nullable(types.boolean())),
  reportOutputStyle: types.optional(
    TextqlRpcPublicPlaybookPlaybookReportStyle$inboundSchema,
  ),
  templateHeaderId: z.optional(z.nullable(types.string())),
  selectedTemplateDataIds: types.optional(z.array(types.string())),
  maxConcurrentTemplates: z.optional(z.nullable(types.number())),
  autoOptimizeConcurrency: z.optional(z.nullable(types.boolean())),
  connectorIds: types.optional(z.array(types.number())),
  templateDataFilters: types.optional(
    z.array(TextqlRpcPublicPlaybookFilterCondition$inboundSchema),
  ),
  parentPlaybookId: z.optional(z.nullable(types.string())),
  originPlaybookId: z.optional(z.nullable(types.string())),
  duplicatedByMemberId: z.optional(z.nullable(types.string())),
  duplicatedAt: types.optional(types.date()),
  chatAccessMode: types.optional(
    TextqlRpcPublicPlaybookChatAccessMode$inboundSchema,
  ),
  chatAccessOverrides: types.optional(
    z.array(TextqlRpcPublicPlaybookChatAccessOverride$inboundSchema),
  ),
  chatIsPublic: types.optional(types.boolean()),
  dashboardIds: types.optional(z.array(types.string())),
  recipientEmailColumn: z.optional(z.nullable(types.string())),
  teamsChannelId: z.optional(z.nullable(types.string())),
  taggedTeamsUserAadIds: types.optional(z.array(types.string())),
  configSource: types.optional(
    TextqlRpcPublicConfigSourceConfigSource$inboundSchema,
  ),
  apiAccessKeyIds: types.optional(z.array(types.string())),
});

export function textqlRpcPublicPlaybookPlaybookFromJSON(
  jsonString: string,
): SafeParseResult<TextqlRpcPublicPlaybookPlaybook, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TextqlRpcPublicPlaybookPlaybook$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextqlRpcPublicPlaybookPlaybook' from JSON`,
  );
}
