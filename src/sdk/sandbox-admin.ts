/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxAdminGetSandbox } from "../funcs/sandbox-admin-get-sandbox.js";
import { sandboxAdminListExecutions } from "../funcs/sandbox-admin-list-executions.js";
import { sandboxAdminListSandboxEgress } from "../funcs/sandbox-admin-list-sandbox-egress.js";
import { sandboxAdminListSandboxFiles } from "../funcs/sandbox-admin-list-sandbox-files.js";
import { sandboxAdminListSandboxSpend } from "../funcs/sandbox-admin-list-sandbox-spend.js";
import { sandboxAdminList } from "../funcs/sandbox-admin-list.js";
import { sandboxAdminReadFile } from "../funcs/sandbox-admin-read-file.js";
import { sandboxAdminRestartSandbox } from "../funcs/sandbox-admin-restart-sandbox.js";
import { sandboxAdminStop } from "../funcs/sandbox-admin-stop.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";

export class SandboxAdmin extends ClientSDK {
  /**
   * GetSandbox
   */
  async getSandbox(
    request: operations.SandboxAdminServiceGetSandboxRequest,
    options?: RequestOptions,
  ): Promise<operations.SandboxAdminServiceGetSandboxResponse> {
    return unwrapAsync(sandboxAdminGetSandbox(
      this,
      request,
      options,
    ));
  }

  /**
   * Outbound HTTP(S) calls a sandbox made (the egress ledger). Durable — reads  the recorded table, so it works for stopped sandboxes too.
   *
   * @remarks
   * Outbound HTTP(S) calls a sandbox made (the egress ledger). Durable — reads
   *  the recorded table, so it works for stopped sandboxes too.
   */
  async listSandboxEgress(
    request: operations.SandboxAdminServiceListSandboxEgressRequest,
    options?: RequestOptions,
  ): Promise<operations.SandboxAdminServiceListSandboxEgressResponse> {
    return unwrapAsync(sandboxAdminListSandboxEgress(
      this,
      request,
      options,
    ));
  }

  /**
   * ListSandboxExecutions
   */
  async listExecutions(
    request: operations.SandboxAdminServiceListSandboxExecutionsRequest,
    options?: RequestOptions,
  ): Promise<operations.SandboxAdminServiceListSandboxExecutionsResponse> {
    return unwrapAsync(sandboxAdminListExecutions(
      this,
      request,
      options,
    ));
  }

  /**
   * Live filesystem of a running sandbox. Both are NO-OP (read-only) and only  return data while the worker is alive; available=false otherwise.
   *
   * @remarks
   * Live filesystem of a running sandbox. Both are NO-OP (read-only) and only
   *  return data while the worker is alive; available=false otherwise.
   */
  async listSandboxFiles(
    request: operations.SandboxAdminServiceListSandboxFilesRequest,
    options?: RequestOptions,
  ): Promise<operations.SandboxAdminServiceListSandboxFilesResponse> {
    return unwrapAsync(sandboxAdminListSandboxFiles(
      this,
      request,
      options,
    ));
  }

  /**
   * Per-lease compute usage for a sandbox, computed from lease durations × the  compute rate. Durable (reads the lease table), so it works for stopped  sandboxes. This is usage (ACUs), not the invoiced dollar amount.
   *
   * @remarks
   * Per-lease compute usage for a sandbox, computed from lease durations × the
   *  compute rate. Durable (reads the lease table), so it works for stopped
   *  sandboxes. This is usage (ACUs), not the invoiced dollar amount.
   */
  async listSandboxSpend(
    request: operations.SandboxAdminServiceListSandboxSpendRequest,
    options?: RequestOptions,
  ): Promise<operations.SandboxAdminServiceListSandboxSpendResponse> {
    return unwrapAsync(sandboxAdminListSandboxSpend(
      this,
      request,
      options,
    ));
  }

  /**
   * ListSandboxes
   */
  async list(
    request: operations.SandboxAdminServiceListSandboxesRequest,
    options?: RequestOptions,
  ): Promise<operations.SandboxAdminServiceListSandboxesResponse> {
    return unwrapAsync(sandboxAdminList(
      this,
      request,
      options,
    ));
  }

  /**
   * ReadSandboxFile
   */
  async readFile(
    request: operations.SandboxAdminServiceReadSandboxFileRequest,
    options?: RequestOptions,
  ): Promise<operations.SandboxAdminServiceReadSandboxFileResponse> {
    return unwrapAsync(sandboxAdminReadFile(
      this,
      request,
      options,
    ));
  }

  /**
   * Restart a stopped/reaped sandbox by re-acquiring a worker for the same  sandbox_id, preserving the original owner. Same scoping as StopSandbox  (owner, or sandbox:write_private for org-wide).
   *
   * @remarks
   * Restart a stopped/reaped sandbox by re-acquiring a worker for the same
   *  sandbox_id, preserving the original owner. Same scoping as StopSandbox
   *  (owner, or sandbox:write_private for org-wide).
   */
  async restartSandbox(
    request: operations.SandboxAdminServiceRestartSandboxRequest,
    options?: RequestOptions,
  ): Promise<operations.SandboxAdminServiceRestartSandboxResponse> {
    return unwrapAsync(sandboxAdminRestartSandbox(
      this,
      request,
      options,
    ));
  }

  /**
   * StopSandbox
   */
  async stop(
    request: operations.SandboxAdminServiceStopSandboxRequest,
    options?: RequestOptions,
  ): Promise<operations.SandboxAdminServiceStopSandboxResponse> {
    return unwrapAsync(sandboxAdminStop(
      this,
      request,
      options,
    ));
  }
}
